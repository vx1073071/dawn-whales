{
  "em": [
    {
      "chan": "em:get-financials",
      "dom": "em",
      "start": 283,
      "end": 290,
      "lines": [
        "  // ── Financial Reports (JVS-41) ─────────────────────────────────────────\r",
        "  ipcMain.handle('em:get-financials', async (_e, code: string, quarters?: number) => {\r",
        "    try {\r",
        "      return await getFinancialReports(code, quarters);\r",
        "    } catch (err: any) {\r",
        "      log.error('[FinancialReports] Error:', err);\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "em:get-valuation",
      "dom": "em",
      "start": 293,
      "end": 300,
      "lines": [
        "  // ── Valuation Data (JVS-42) ────────────────────────────────────────────\r",
        "  ipcMain.handle('em:get-valuation', async (_e, code: string, historyDays?: number) => {\r",
        "    try {\r",
        "      return await getValuationData(code, historyDays);\r",
        "    } catch (err: any) {\r",
        "      log.error('[ValuationData] Error:', err);\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "em:price-option",
      "dom": "em",
      "start": 366,
      "end": 373,
      "lines": [
        "  // ── Options Pricing Engine (JVS-44) ────────────────────────────────────\r",
        "  ipcMain.handle('em:price-option', async (_e, params: any) => {\r",
        "    try {\r",
        "      return { success: true, ...blackScholesPrice(params) };\r",
        "    } catch (err: any) {\r",
        "      log.error('[OptionsPricing] Price error:', err);\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "em:calc-greeks",
      "dom": "em",
      "start": 375,
      "end": 382,
      "lines": [
        "  ipcMain.handle('em:calc-greeks', async (_e, params: any) => {\r",
        "    try {\r",
        "      return { success: true, ...calculateGreeks(params) };\r",
        "    } catch (err: any) {\r",
        "      log.error('[OptionsPricing] Greeks error:', err);\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "em:implied-vol",
      "dom": "em",
      "start": 384,
      "end": 391,
      "lines": [
        "  ipcMain.handle('em:implied-vol', async (_e, marketPrice: number, S: number, K: number, T: number, r: number, optionType: string, q?: number) => {\r",
        "    try {\r",
        "      return { success: true, ...impliedVolatility(marketPrice, S, K, T, r, optionType as any, q) };\r",
        "    } catch (err: any) {\r",
        "      log.error('[OptionsPricing] IV error:', err);\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "em:vol-surface",
      "dom": "em",
      "start": 393,
      "end": 401,
      "lines": [
        "  ipcMain.handle('em:vol-surface', async (_e, S: number, r: number, strikes: number[], expiries: number[], callPrices: number[][], putPrices?: number[][]) => {\r",
        "    try {\r",
        "      const surface = buildVolSurface(S, r, strikes, expiries, callPrices, putPrices);\r",
        "      return { success: true, surface };\r",
        "    } catch (err: any) {\r",
        "      log.error('[OptionsPricing] Surface error:', err);\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "em:price-and-greeks",
      "dom": "em",
      "start": 403,
      "end": 410,
      "lines": [
        "  ipcMain.handle('em:price-and-greeks', async (_e, params: any) => {\r",
        "    try {\r",
        "      return { success: true, ...priceAndGreeks(params) };\r",
        "    } catch (err: any) {\r",
        "      log.error('[OptionsPricing] Price+Greeks error:', err);\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "em:calc-risk-metrics",
      "dom": "em",
      "start": 413,
      "end": 421,
      "lines": [
        "  // ── Risk Metrics Calculator (JVS-46) ───────────────────────────────────\r",
        "  ipcMain.handle('em:calc-risk-metrics', async (_e, params: any) => {\r",
        "    try {\r",
        "      const result = calculateRiskMetrics(params);\r",
        "      return { success: true, metrics: result };\r",
        "    } catch (err: any) {\r",
        "      log.error('[RiskMetrics] Error:', err);\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "em:calc-sharpe",
      "dom": "em",
      "start": 423,
      "end": 429,
      "lines": [
        "  ipcMain.handle('em:calc-sharpe', async (_e, returns: number[], riskFreeRate?: number, tradingDays?: number) => {\r",
        "    try {\r",
        "      return { success: true, sharpe: calcSharpeRatio(returns, riskFreeRate, tradingDays) };\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "em:calc-max-drawdown",
      "dom": "em",
      "start": 431,
      "end": 437,
      "lines": [
        "  ipcMain.handle('em:calc-max-drawdown', async (_e, returns: number[]) => {\r",
        "    try {\r",
        "      return { success: true, maxDrawdown: calcMaxDrawdown(returns) };\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "em:calc-var",
      "dom": "em",
      "start": 439,
      "end": 445,
      "lines": [
        "  ipcMain.handle('em:calc-var', async (_e, returns: number[], confidence?: number) => {\r",
        "    try {\r",
        "      return { success: true, var: calcVaR(returns, confidence) };\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "em:portfolio-attribution",
      "dom": "em",
      "start": 448,
      "end": 456,
      "lines": [
        "  // ── Performance Attribution (JVS-45) ─────────────────────────────────\r",
        "  ipcMain.handle('em:portfolio-attribution', async (_e, params: any) => {\r",
        "    try {\r",
        "      const result = brinsonAttribution(params);\r",
        "      return { success: true, result };\r",
        "    } catch (err: any) {\r",
        "      log.error('[Attribution] Error:', err);\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "em:time-series-attribution",
      "dom": "em",
      "start": 458,
      "end": 466,
      "lines": [
        "  ipcMain.handle('em:time-series-attribution', async (_e, params: any) => {\r",
        "    try {\r",
        "      const result = timeSeriesAttribution(params);\r",
        "      return { success: true, result };\r",
        "    } catch (err: any) {\r",
        "      log.error('[Attribution] TimeSeries Error:', err);\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "em:correlation-matrix",
      "dom": "em",
      "start": 469,
      "end": 477,
      "lines": [
        "  // ── Correlation Matrix v2 (JVS-47) ──────────────────────────────────────\r",
        "  ipcMain.handle('em:correlation-matrix', async (_e, params: any) => {\r",
        "    try {\r",
        "      const result = correlationMatrix(params);\r",
        "      return { success: true, result };\r",
        "    } catch (err: any) {\r",
        "      log.error('[CorrelationMatrix] Error:', err);\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "em:sector-rotation",
      "dom": "em",
      "start": 480,
      "end": 488,
      "lines": [
        "  // ── Sector Rotation v2 (JVS-48) ─────────────────────────────────────────\r",
        "  ipcMain.handle('em:sector-rotation', async (_e, params: any) => {\r",
        "    try {\r",
        "      const result = detectSectorRotation(params);\r",
        "      return { success: true, result };\r",
        "    } catch (err: any) {\r",
        "      log.error('[SectorRotation] Error:', err);\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "em:get-heatmap",
      "dom": "em",
      "start": 2966,
      "end": 2976,
      "lines": [
        "  // ── EM Data Provider — Sector Heatmap (JVS-1) ─────────────────────────\r",
        "  ipcMain.handle('em:get-heatmap', async (_e, boardType?: string, limit?: number) => {\r",
        "    if (!emDataProvider) return { success: false, error: 'EMDataProvider not initialized' };\r",
        "    try {\r",
        "      const bt = (boardType === 'concept' || boardType === 'region') ? boardType : 'industry';\r",
        "      const result = await emDataProvider.getHeatmap(bt, limit || 50);\r",
        "      return { success: true, ...result };\r",
        "    } catch (err: any) {\r",
        "      log.error('[EMDataProvider] Heatmap fetch failed:', err.message);\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "em:get-all-heatmaps",
      "dom": "em",
      "start": 2978,
      "end": 2987,
      "lines": [
        "  ipcMain.handle('em:get-all-heatmaps', async () => {\r",
        "    if (!emDataProvider) return { success: false, error: 'EMDataProvider not initialized' };\r",
        "    try {\r",
        "      const maps = await emDataProvider.getAllHeatmaps();\r",
        "      return { success: true, ...maps };\r",
        "    } catch (err: any) {\r",
        "      log.error('[EMDataProvider] All heatmaps fetch failed:', err.message);\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "em:get-macro",
      "dom": "em",
      "start": 2990,
      "end": 3000,
      "lines": [
        "  // ── Macro Data Provider — Dashboard (JVS-2) ───────────────────────────\r",
        "  ipcMain.handle('em:get-macro', async (_e, indicator?: string, limit?: number) => {\r",
        "    if (!macroDataProvider) return { success: false, error: 'MacroDataProvider not initialized' };\r",
        "    try {\r",
        "      const type = (indicator || 'GDP') as any;\r",
        "      const result = await macroDataProvider.getIndicator(type, limit || 24);\r",
        "      return { success: true, data: result };\r",
        "    } catch (err: any) {\r",
        "      log.error('[MacroDataProvider] Fetch failed:', err.message);\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "em:get-macro-dashboard",
      "dom": "em",
      "start": 3002,
      "end": 3011,
      "lines": [
        "  ipcMain.handle('em:get-macro-dashboard', async (_e, indicators?: string[]) => {\r",
        "    if (!macroDataProvider) return { success: false, error: 'MacroDataProvider not initialized' };\r",
        "    try {\r",
        "      const result = await macroDataProvider.getDashboard(indicators as any);\r",
        "      return { success: true, ...result };\r",
        "    } catch (err: any) {\r",
        "      log.error('[MacroDataProvider] Dashboard fetch failed:', err.message);\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "em:get-sentiment",
      "dom": "em",
      "start": 3014,
      "end": 3024,
      "lines": [
        "  // ── Sentiment Index Engine (JVS-3) ──────────────────────────────────────\r",
        "  ipcMain.handle('em:get-sentiment', async (_e, input?: any) => {\r",
        "    try {\r",
        "      const engine = new SentimentIndexEngine();\r",
        "      const sentimentInput = input || {};\r",
        "      const result = engine.compute(sentimentInput);\r",
        "      return { success: true, result };\r",
        "    } catch (err: any) {\r",
        "      log.error('[SentimentIndex] Compute failed:', err.message);\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "em:get-news-aggregate",
      "dom": "em",
      "start": 3039,
      "end": 3048,
      "lines": [
        "  // ── News Aggregator (JVS-5) ────────────────────────────────────────────\r",
        "  ipcMain.handle('em:get-news-aggregate', async (_e, request: any) => {\r",
        "    if (!newsAggregator) return { success: false, error: 'NewsAggregator not initialized' };\r",
        "    try {\r",
        "      const result = await newsAggregator.search(request || { query: '' });\r",
        "      return { success: true, ...result };\r",
        "    } catch (err: any) {\r",
        "      log.error('[NewsAggregator] Search failed:', err.message);\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "em:get-market-mood",
      "dom": "em",
      "start": 3050,
      "end": 3059,
      "lines": [
        "  ipcMain.handle('em:get-market-mood', async (_e, symbols?: string[]) => {\r",
        "    if (!newsAggregator) return { success: false, error: 'NewsAggregator not initialized' };\r",
        "    try {\r",
        "      const report = await newsAggregator.getMarketMood(symbols);\r",
        "      return { success: true, report };\r",
        "    } catch (err: any) {\r",
        "      log.error('[NewsAggregator] Market mood failed:', err.message);\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "em:get-sector-rotation",
      "dom": "em",
      "start": 3062,
      "end": 3071,
      "lines": [
        "  // ── Sector Rotation Monitor (JVS-6) ──────────────────────────────────\r",
        "  ipcMain.handle('em:get-sector-rotation', async () => {\r",
        "    if (!sectorRotation) return { success: false, error: 'SectorRotation not initialized' };\r",
        "    try {\r",
        "      const report = sectorRotation.analyze();\r",
        "      return { success: true, ...report };\r",
        "    } catch (err: any) {\r",
        "      log.error('[SectorRotation] Analyze failed:', err.message);\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "em:record-sector-snapshot",
      "dom": "em",
      "start": 3073,
      "end": 3081,
      "lines": [
        "  ipcMain.handle('em:record-sector-snapshot', async (_e, sectors: any[]) => {\r",
        "    if (!sectorRotation) return { success: false, error: 'SectorRotation not initialized' };\r",
        "    try {\r",
        "      sectorRotation.recordSnapshot(sectors || []);\r",
        "      return { success: true };\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "em:get-anomaly-summary",
      "dom": "em",
      "start": 3084,
      "end": 3092,
      "lines": [
        "  // ── Stock Anomaly Detector (JVS-7) ────────────────────────────────────\r",
        "  ipcMain.handle('em:get-anomaly-summary', async () => {\r",
        "    if (!stockAnomalyDetector) return { success: false, error: 'AnomalyDetector not initialized' };\r",
        "    try {\r",
        "      const summary = stockAnomalyDetector.getSummary();\r",
        "      return { success: true, summary };\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "em:get-anomaly-alerts",
      "dom": "em",
      "start": 3094,
      "end": 3102,
      "lines": [
        "  ipcMain.handle('em:get-anomaly-alerts', async (_e, options?: any) => {\r",
        "    if (!stockAnomalyDetector) return { success: false, error: 'AnomalyDetector not initialized' };\r",
        "    try {\r",
        "      const alerts = stockAnomalyDetector.getAlerts(options);\r",
        "      return { success: true, alerts };\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "em:process-anomaly-quotes",
      "dom": "em",
      "start": 3104,
      "end": 3112,
      "lines": [
        "  ipcMain.handle('em:process-anomaly-quotes', async (_e, quotes: any[]) => {\r",
        "    if (!stockAnomalyDetector) return { success: false, error: 'AnomalyDetector not initialized' };\r",
        "    try {\r",
        "      const newAlerts = stockAnomalyDetector.processQuotes(quotes || []);\r",
        "      return { success: true, newAlerts: newAlerts.length };\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "em:acknowledge-anomaly",
      "dom": "em",
      "start": 3114,
      "end": 3122,
      "lines": [
        "  ipcMain.handle('em:acknowledge-anomaly', async (_e, id: string) => {\r",
        "    if (!stockAnomalyDetector) return { success: false, error: 'AnomalyDetector not initialized' };\r",
        "    try {\r",
        "      const result = stockAnomalyDetector.acknowledgeAlert(id);\r",
        "      return { success: true, result };\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "em:get-hotspot",
      "dom": "em",
      "start": 3125,
      "end": 3134,
      "lines": [
        "  // ── Market Hotspot (JVS-8) ─────────────────────────────────────────────\r",
        "  ipcMain.handle('em:get-hotspot', async (_e, query?: any) => {\r",
        "    if (!marketHotspot) return { success: false, error: 'MarketHotspot not initialized' };\r",
        "    try {\r",
        "      const report = await marketHotspot.getReport(query);\r",
        "      return { success: true, ...report };\r",
        "    } catch (err: any) {\r",
        "      log.error('[MarketHotspot] Fetch failed:', err.message);\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "em:get-dragon-tiger",
      "dom": "em",
      "start": 3197,
      "end": 3204,
      "lines": [
        "  // ── Dragon Tiger List — 龙虎榜 (JVS-10) ─────────────────────────────\r",
        "  ipcMain.handle('em:get-dragon-tiger', async (_e, date?: string) => {\r",
        "    try {\r",
        "      const result = await getDragonTigerList(date);\r",
        "      return result;\r",
        "    } catch (err: any) {\r",
        "      return { success: false, entries: [], total: 0, date: '', error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "em:get-dragon-tiger-detail",
      "dom": "em",
      "start": 3206,
      "end": 3213,
      "lines": [
        "  ipcMain.handle('em:get-dragon-tiger-detail', async (_e, code: string, date: string) => {\r",
        "    try {\r",
        "      const detail = await getDragonTigerDetail(code, date);\r",
        "      return { success: !!detail, detail };\r",
        "    } catch (err: any) {\r",
        "      return { success: false, detail: null, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "em:get-institutional-trades",
      "dom": "em",
      "start": 3215,
      "end": 3222,
      "lines": [
        "  ipcMain.handle('em:get-institutional-trades', async (_e, date?: string) => {\r",
        "    try {\r",
        "      const entries = await getInstitutionalTrades(date);\r",
        "      return { success: true, entries };\r",
        "    } catch (err: any) {\r",
        "      return { success: false, entries: [], error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "em:get-capital-flow-stock",
      "dom": "em",
      "start": 3225,
      "end": 3232,
      "lines": [
        "  // ── Capital Flow Ranking — 资金流排行 (JVS-11) ──────────────────────\r",
        "  ipcMain.handle('em:get-capital-flow-stock', async (_e, sortBy?: string, order?: string, limit?: number) => {\r",
        "    try {\r",
        "      const result = await getStockCapitalFlowRank(sortBy as any, order as any, limit);\r",
        "      return result;\r",
        "    } catch (err: any) {\r",
        "      return { success: false, items: [], total: 0, type: 'stock', error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "em:get-capital-flow-sector",
      "dom": "em",
      "start": 3234,
      "end": 3241,
      "lines": [
        "  ipcMain.handle('em:get-capital-flow-sector', async (_e, sortBy?: string, order?: string, limit?: number) => {\r",
        "    try {\r",
        "      const result = await getSectorCapitalFlowRank(sortBy as any, order as any, limit);\r",
        "      return result;\r",
        "    } catch (err: any) {\r",
        "      return { success: false, items: [], total: 0, type: 'sector', error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "em:get-capital-flow-concept",
      "dom": "em",
      "start": 3243,
      "end": 3250,
      "lines": [
        "  ipcMain.handle('em:get-capital-flow-concept', async (_e, sortBy?: string, order?: string, limit?: number) => {\r",
        "    try {\r",
        "      const result = await getConceptCapitalFlowRank(sortBy as any, order as any, limit);\r",
        "      return result;\r",
        "    } catch (err: any) {\r",
        "      return { success: false, items: [], total: 0, type: 'concept', error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "em:get-capital-flow-alerts",
      "dom": "em",
      "start": 3253,
      "end": 3261,
      "lines": [
        "  // ── Capital Flow Monitor — Real-time alerts (JVS-12) ─────────────────\r",
        "  ipcMain.handle('em:get-capital-flow-alerts', async (_e, items?: any[]) => {\r",
        "    try {\r",
        "      const monitor = getCapitalFlowMonitor();\r",
        "      const alerts = items ? monitor.process(items) : [];\r",
        "      return { success: true, alerts, config: monitor.getConfig() };\r",
        "    } catch (err: any) {\r",
        "      return { success: false, alerts: [], error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "em:set-capital-flow-config",
      "dom": "em",
      "start": 3263,
      "end": 3271,
      "lines": [
        "  ipcMain.handle('em:set-capital-flow-config', async (_e, config: any) => {\r",
        "    try {\r",
        "      const monitor = getCapitalFlowMonitor();\r",
        "      monitor.updateConfig(config);\r",
        "      return { success: true, config: monitor.getConfig() };\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "em:clear-capital-flow-history",
      "dom": "em",
      "start": 3273,
      "end": 3280,
      "lines": [
        "  ipcMain.handle('em:clear-capital-flow-history', async () => {\r",
        "    try {\r",
        "      getCapitalFlowMonitor().clearHistory();\r",
        "      return { success: true };\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "em:get-fund-holdings",
      "dom": "em",
      "start": 3283,
      "end": 3290,
      "lines": [
        "  // ── Fund Holdings — 基金持仓数据 (JVS-13) ────────────────────────\r",
        "  ipcMain.handle('em:get-fund-holdings', async (_e, fundCode: string, reportDate?: string) => {\r",
        "    try {\r",
        "      const result = await getFundHoldings(fundCode, reportDate);\r",
        "      return result;\r",
        "    } catch (err: any) {\r",
        "      return { success: false, items: [], total: 0, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "em:get-stock-fund-ownership",
      "dom": "em",
      "start": 3292,
      "end": 3299,
      "lines": [
        "  ipcMain.handle('em:get-stock-fund-ownership', async (_e, stockCode: string, reportDate?: string) => {\r",
        "    try {\r",
        "      const result = await getStockFundOwnership(stockCode, reportDate);\r",
        "      return result;\r",
        "    } catch (err: any) {\r",
        "      return { success: false, items: [], total: 0, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "em:get-fund-increase-rank",
      "dom": "em",
      "start": 3301,
      "end": 3308,
      "lines": [
        "  ipcMain.handle('em:get-fund-increase-rank', async (_e, limit?: number, reportDate?: string) => {\r",
        "    try {\r",
        "      const result = await getFundIncreaseRank(limit, reportDate);\r",
        "      return result;\r",
        "    } catch (err: any) {\r",
        "      return { success: false, items: [], total: 0, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "em:get-fund-decrease-rank",
      "dom": "em",
      "start": 3310,
      "end": 3317,
      "lines": [
        "  ipcMain.handle('em:get-fund-decrease-rank', async (_e, limit?: number, reportDate?: string) => {\r",
        "    try {\r",
        "      const result = await getFundDecreaseRank(limit, reportDate);\r",
        "      return result;\r",
        "    } catch (err: any) {\r",
        "      return { success: false, items: [], total: 0, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "em:diagnose-stock",
      "dom": "em",
      "start": 3320,
      "end": 3327,
      "lines": [
        "  // ── Stock Diagnosis — 个股诊断聚合器 (JVS-14) ──────────────────────\r",
        "  ipcMain.handle('em:diagnose-stock', async (_e, request: any) => {\r",
        "    try {\r",
        "      const result = await diagnoseStock(request || { code: '' });\r",
        "      return result;\r",
        "    } catch (err: any) {\r",
        "      return { success: false, code: '', name: '', timestamp: Date.now(), error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "em:batch-diagnose",
      "dom": "em",
      "start": 3329,
      "end": 3336,
      "lines": [
        "  ipcMain.handle('em:batch-diagnose', async (_e, codes: string[], options?: any) => {\r",
        "    try {\r",
        "      const results = await batchDiagnose(codes || [], options);\r",
        "      return { success: true, reports: results };\r",
        "    } catch (err: any) {\r",
        "      return { success: false, reports: [], error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "em:portfolio-risk",
      "dom": "em",
      "start": 3339,
      "end": 3346,
      "lines": [
        "  // ── Portfolio Risk — 组合风险计算器 (JVS-15) ─────────────────────\r",
        "  ipcMain.handle('em:portfolio-risk', async (_e, request: any) => {\r",
        "    try {\r",
        "      const result = await calculatePortfolioRisk(request || { positions: [] });\r",
        "      return result;\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "em:get-market-breadth",
      "dom": "em",
      "start": 3349,
      "end": 3356,
      "lines": [
        "  // ── Market Breadth — 市场广度分析器 (JVS-16) ────────────────────\r",
        "  ipcMain.handle('em:get-market-breadth', async () => {\r",
        "    try {\r",
        "      const result = await getMarketBreadth();\r",
        "      return result;\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "em:get-consumer-data",
      "dom": "em",
      "start": 3359,
      "end": 3366,
      "lines": [
        "  // ── Consumer Data — 消费者数据服务 (JVS-17) ────────────────────\r",
        "  ipcMain.handle('em:get-consumer-data', async (_e, months?: number) => {\r",
        "    try {\r",
        "      const result = await getConsumerDataReport(months || 12);\r",
        "      return result;\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "em:get-margin-data",
      "dom": "em",
      "start": 3369,
      "end": 3376,
      "lines": [
        "  // ── Margin Data — 融资融券数据服务 (JVS-18) ────────────────────\r",
        "  ipcMain.handle('em:get-margin-data', async () => {\r",
        "    try {\r",
        "      const result = await getMarginDataReport();\r",
        "      return result;\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "em:get-stock-margin",
      "dom": "em",
      "start": 3378,
      "end": 3385,
      "lines": [
        "  ipcMain.handle('em:get-stock-margin', async (_e, code: string, days?: number) => {\r",
        "    try {\r",
        "      const result = await getStockMargin(code, days || 30);\r",
        "      return { success: true, data: result };\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "em:get-margin-balance-rank",
      "dom": "em",
      "start": 3387,
      "end": 3394,
      "lines": [
        "  ipcMain.handle('em:get-margin-balance-rank', async (_e, limit?: number) => {\r",
        "    try {\r",
        "      const result = await getMarginBalanceRanking(limit || 30);\r",
        "      return { success: true, data: result };\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "em:get-short-interest-rank",
      "dom": "em",
      "start": 3396,
      "end": 3403,
      "lines": [
        "  ipcMain.handle('em:get-short-interest-rank', async (_e, limit?: number) => {\r",
        "    try {\r",
        "      const result = await getShortInterestRanking(limit || 30);\r",
        "      return { success: true, data: result };\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "em:get-stock-overview",
      "dom": "em",
      "start": 3406,
      "end": 3414,
      "lines": [
        "  // ── EMI Unified Service Layer (JVS-19) ──────────────────────────────────\r",
        "  ipcMain.handle('em:get-stock-overview', async (_e, code: string) => {\r",
        "    if (!code) return { success: false, error: 'Stock code required' };\r",
        "    try {\r",
        "      const result = await getStockOverview(code);\r",
        "      return { success: true, data: result };\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "em:get-market-overview",
      "dom": "em",
      "start": 3416,
      "end": 3423,
      "lines": [
        "  ipcMain.handle('em:get-market-overview', async () => {\r",
        "    try {\r",
        "      const result = await getMarketOverview();\r",
        "      return { success: true, data: result };\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "em:get-daily-report",
      "dom": "em",
      "start": 3425,
      "end": 3432,
      "lines": [
        "  ipcMain.handle('em:get-daily-report', async () => {\r",
        "    try {\r",
        "      const result = await getDailyReport();\r",
        "      return { success: true, data: result };\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "em:dragon-tiger-stream-start",
      "dom": "em",
      "start": 3942,
      "end": 3955,
      "lines": [
        "  // ── Dragon Tiger Stream (JVS-22 PM) ─────────────────────────────────────\r",
        "  ipcMain.handle('em:dragon-tiger-stream-start', async () => {\r",
        "    try {\r",
        "      const stream = getDragonTigerStream();\r",
        "      stream.start();\r",
        "      stream.on('update', (event) => {\r",
        "        if (mainWindow && !mainWindow.isDestroyed()) {\r",
        "          mainWindow.webContents.send('dragon-tiger:update', event);\r",
        "        }\r",
        "      });\r",
        "      return { success: true };\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "em:dragon-tiger-stream-stop",
      "dom": "em",
      "start": 3957,
      "end": 3964,
      "lines": [
        "  ipcMain.handle('em:dragon-tiger-stream-stop', async () => {\r",
        "    try {\r",
        "      getDragonTigerStream().stop();\r",
        "      return { success: true };\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "em:dragon-tiger-stream-fetch",
      "dom": "em",
      "start": 3966,
      "end": 3973,
      "lines": [
        "  ipcMain.handle('em:dragon-tiger-stream-fetch', async () => {\r",
        "    try {\r",
        "      const result = await getDragonTigerStream().fetchNow();\r",
        "      return { success: true, data: result };\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "em:dragon-tiger-stream-status",
      "dom": "em",
      "start": 3975,
      "end": 3981,
      "lines": [
        "  ipcMain.handle('em:dragon-tiger-stream-status', async () => {\r",
        "    try {\r",
        "      return { success: true, status: getDragonTigerStream().getStatus() };\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "em:get-unlock-calendar",
      "dom": "em",
      "start": 3984,
      "end": 3991,
      "lines": [
        "  // ── Unlock Calendar (JVS-23 PM) ────────────────────────────────────────\r",
        "  ipcMain.handle('em:get-unlock-calendar', async (_e, days?: number) => {\r",
        "    try {\r",
        "      const result = await getUnlockCalendar(days || 30);\r",
        "      return result;\r",
        "    } catch (err: any) {\r",
        "      return { success: false, events: [], total: 0, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "em:get-dividend-calendar",
      "dom": "em",
      "start": 3994,
      "end": 4001,
      "lines": [
        "  // ── Dividend Calendar (JVS-24 PM) ──────────────────────────────────────\r",
        "  ipcMain.handle('em:get-dividend-calendar', async (_e, days?: number) => {\r",
        "    try {\r",
        "      const result = await getDividendCalendar(days || 30);\r",
        "      return result;\r",
        "    } catch (err: any) {\r",
        "      return { success: false, events: [], total: 0, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "em:get-earnings-calendar",
      "dom": "em",
      "start": 4004,
      "end": 4011,
      "lines": [
        "  // ── Earnings Calendar (JVS-25 PM) ──────────────────────────────────────\r",
        "  ipcMain.handle('em:get-earnings-calendar', async (_e, days?: number) => {\r",
        "    try {\r",
        "      const result = await getEarningsCalendar(days || 30);\r",
        "      return result;\r",
        "    } catch (err: any) {\r",
        "      return { success: false, events: [], total: 0, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "em:export-data",
      "dom": "em",
      "start": 4014,
      "end": 4021,
      "lines": [
        "  // ── Data Exporter (JVS-26 PM) ──────────────────────────────────────────\r",
        "  ipcMain.handle('em:export-data', async (_e, type: string, format?: string) => {\r",
        "    try {\r",
        "      const result = await exportData(type as any, (format as any) || 'json');\r",
        "      return result;\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "em:smart-pick",
      "dom": "em",
      "start": 4024,
      "end": 4032,
      "lines": [
        "  // ── Smart Picker (JVS-25 PM Round 2) ───────────────────────────────────\r",
        "  ipcMain.handle('em:smart-pick', async (_e, request?: any) => {\r",
        "    try {\r",
        "      const picker = getSmartPicker();\r",
        "      const result = await picker.pick(request || {});\r",
        "      return result;\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "em:backfill-start",
      "dom": "em",
      "start": 4086,
      "end": 4094,
      "lines": [
        "  // ── History Backfill (JVS-30) ──────────────────────────────────────────\r",
        "  ipcMain.handle('em:backfill-start', async (_e, config?: any) => {\r",
        "    try {\r",
        "      const backfill = getHistoryBackfill();\r",
        "      const status = await backfill.start(config);\r",
        "      return { success: true, status };\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "em:backfill-status",
      "dom": "em",
      "start": 4096,
      "end": 4102,
      "lines": [
        "  ipcMain.handle('em:backfill-status', async () => {\r",
        "    try {\r",
        "      return { success: true, status: getHistoryBackfill().getStatus() };\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "em:backfill-data",
      "dom": "em",
      "start": 4104,
      "end": 4111,
      "lines": [
        "  ipcMain.handle('em:backfill-data', async (_e, module: string) => {\r",
        "    try {\r",
        "      const data = getHistoryBackfill().getBackfillData(module);\r",
        "      return { success: true, data };\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "em:backfill-list",
      "dom": "em",
      "start": 4113,
      "end": 4120,
      "lines": [
        "  ipcMain.handle('em:backfill-list', async () => {\r",
        "    try {\r",
        "      const files = getHistoryBackfill().listBackfillFiles();\r",
        "      return { success: true, files };\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    }
  ],
  "indicator": [
    {
      "chan": "indicator:compute",
      "dom": "indicator",
      "start": 303,
      "end": 310,
      "lines": [
        "  // ── Technical Indicators (JVS-43) ──────────────────────────────────────\r",
        "  ipcMain.handle('indicator:compute', async (_e, klines: any[], indicators?: string[], options?: any) => {\r",
        "    try {\r",
        "      return computeIndicators(klines, indicators, options);\r",
        "    } catch (err: any) {\r",
        "      log.error('[TechnicalIndicators] Error:', err);\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "indicator:realtime-add",
      "dom": "indicator",
      "start": 313,
      "end": 321,
      "lines": [
        "  // ── Realtime Technical Indicators (JVS-36) ─────────────────────────────\r",
        "  ipcMain.handle('indicator:realtime-add', async (_e, symbol: string, kline: any) => {\r",
        "    try {\r",
        "      const calculator = getRealtimeIndicatorCalculator();\r",
        "      return { success: true, indicators: calculator.addKLine(symbol, kline) };\r",
        "    } catch (err: any) {\r",
        "      log.error('[RealtimeIndicators] Add error:', err);\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "indicator:realtime-add-batch",
      "dom": "indicator",
      "start": 323,
      "end": 331,
      "lines": [
        "  ipcMain.handle('indicator:realtime-add-batch', async (_e, symbol: string, klines: any[]) => {\r",
        "    try {\r",
        "      const calculator = getRealtimeIndicatorCalculator();\r",
        "      return { success: true, indicators: calculator.addKLines(symbol, klines) };\r",
        "    } catch (err: any) {\r",
        "      log.error('[RealtimeIndicators] Batch add error:', err);\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "indicator:realtime-get-buffer",
      "dom": "indicator",
      "start": 333,
      "end": 341,
      "lines": [
        "  ipcMain.handle('indicator:realtime-get-buffer', async (_e, symbol: string) => {\r",
        "    try {\r",
        "      const calculator = getRealtimeIndicatorCalculator();\r",
        "      return { success: true, klines: calculator.getKLineBuffer(symbol) };\r",
        "    } catch (err: any) {\r",
        "      log.error('[RealtimeIndicators] Get buffer error:', err);\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "indicator:realtime-clear",
      "dom": "indicator",
      "start": 343,
      "end": 352,
      "lines": [
        "  ipcMain.handle('indicator:realtime-clear', async (_e, symbol: string) => {\r",
        "    try {\r",
        "      const calculator = getRealtimeIndicatorCalculator();\r",
        "      calculator.clearBuffer(symbol);\r",
        "      return { success: true };\r",
        "    } catch (err: any) {\r",
        "      log.error('[RealtimeIndicators] Clear error:', err);\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "indicator:realtime-clear-all",
      "dom": "indicator",
      "start": 354,
      "end": 363,
      "lines": [
        "  ipcMain.handle('indicator:realtime-clear-all', async () => {\r",
        "    try {\r",
        "      const calculator = getRealtimeIndicatorCalculator();\r",
        "      calculator.clearAllBuffers();\r",
        "      return { success: true };\r",
        "    } catch (err: any) {\r",
        "      log.error('[RealtimeIndicators] Clear all error:', err);\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    }
  ],
  "sentiment": [
    {
      "chan": "sentiment:attribution",
      "dom": "sentiment",
      "start": 492,
      "end": 500,
      "lines": [
        "  ipcMain.handle('sentiment:attribution', async (_e, params: any) => {\r",
        "    try {\r",
        "      const result = sentimentAttrEngine.attributeSentiment(params);\r",
        "      return { success: true, result };\r",
        "    } catch (err: any) {\r",
        "      log.error('[SentimentAttr] Error:', err);\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "sentiment:stream-start",
      "dom": "sentiment",
      "start": 3652,
      "end": 3675,
      "lines": [
        "  // ── Realtime Sentiment Stream (JVS-33) ─────────────────────────────────\r",
        "  ipcMain.handle('sentiment:stream-start', async () => {\r",
        "    try {\r",
        "      const stream = getRealtimeSentimentStream();\r",
        "      stream.start();\r",
        "      \r",
        "      // Forward ticks to renderer\r",
        "      stream.on('tick', (tick) => {\r",
        "        if (mainWindow && !mainWindow.isDestroyed()) {\r",
        "          mainWindow.webContents.send('sentiment:stream-tick', tick);\r",
        "        }\r",
        "      });\r",
        "      \r",
        "      // Forward alerts to renderer\r",
        "      stream.on('alert', (alert) => {\r",
        "        if (mainWindow && !mainWindow.isDestroyed()) {\r",
        "          mainWindow.webContents.send('sentiment:stream-alert', alert);\r",
        "        }\r",
        "      });\r",
        "      \r",
        "      return { success: true };\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "sentiment:stream-stop",
      "dom": "sentiment",
      "start": 3677,
      "end": 3685,
      "lines": [
        "  ipcMain.handle('sentiment:stream-stop', async () => {\r",
        "    try {\r",
        "      const stream = getRealtimeSentimentStream();\r",
        "      stream.stop();\r",
        "      return { success: true };\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "sentiment:stream-status",
      "dom": "sentiment",
      "start": 3687,
      "end": 3695,
      "lines": [
        "  ipcMain.handle('sentiment:stream-status', async () => {\r",
        "    try {\r",
        "      const stream = getRealtimeSentimentStream();\r",
        "      const current = stream.getCurrentSentiment();\r",
        "      return { success: true, current };\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "sentiment:stream-history",
      "dom": "sentiment",
      "start": 3697,
      "end": 3706,
      "lines": [
        "  ipcMain.handle('sentiment:stream-history', async (_e, limit?: number) => {\r",
        "    try {\r",
        "      const stream = getRealtimeSentimentStream();\r",
        "      const history = stream.getHistory();\r",
        "      const limited = limit ? history.slice(-limit) : history;\r",
        "      return { success: true, history: limited };\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "sentiment:stream-alerts",
      "dom": "sentiment",
      "start": 3708,
      "end": 3716,
      "lines": [
        "  ipcMain.handle('sentiment:stream-alerts', async () => {\r",
        "    try {\r",
        "      const stream = getRealtimeSentimentStream();\r",
        "      const alerts = stream.getAlerts();\r",
        "      return { success: true, alerts };\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "sentiment:stream-clear-alerts",
      "dom": "sentiment",
      "start": 3718,
      "end": 3726,
      "lines": [
        "  ipcMain.handle('sentiment:stream-clear-alerts', async () => {\r",
        "    try {\r",
        "      const stream = getRealtimeSentimentStream();\r",
        "      stream.clearAlerts();\r",
        "      return { success: true };\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "sentiment:dashboard",
      "dom": "sentiment",
      "start": 3767,
      "end": 3774,
      "lines": [
        "  // ── Sentiment Dashboard API (JVS-36) ──────────────────────────────────\r",
        "  ipcMain.handle('sentiment:dashboard', async () => {\r",
        "    try {\r",
        "      const dashboard = getSentimentDashboard();\r",
        "      return { success: true, dashboard };\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    }
  ],
  "data": [
    {
      "chan": "predict:capital-flow",
      "dom": "predict",
      "start": 504,
      "end": 512,
      "lines": [
        "  ipcMain.handle('predict:capital-flow', async (_e, params: any) => {\r",
        "    try {\r",
        "      const result = flowPredictor.predict(params);\r",
        "      return { success: true, result };\r",
        "    } catch (err: any) {\r",
        "      log.error('[FlowPredict] Error:', err);\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "data:valuation-dashboard",
      "dom": "data",
      "start": 563,
      "end": 571,
      "lines": [
        "  // ── Valuation Dashboard (JVS-49) ────────────────────────────────────────\r",
        "  ipcMain.handle('data:valuation-dashboard', async (_e, codes: string[], historyDays?: number) => {\r",
        "    try {\r",
        "      const result = await getValuationDashboard(codes, historyDays);\r",
        "      return { success: true, result };\r",
        "    } catch (err: any) {\r",
        "      log.error('[ValuationDashboard] Error:', err);\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "data:valuation-dashboard-batch",
      "dom": "data",
      "start": 573,
      "end": 581,
      "lines": [
        "  ipcMain.handle('data:valuation-dashboard-batch', async (_e, codes: string[], batchSize?: number, delayMs?: number) => {\r",
        "    try {\r",
        "      const result = await getValuationDashboardBatch(codes, batchSize, delayMs);\r",
        "      return { success: true, result };\r",
        "    } catch (err: any) {\r",
        "      log.error('[ValuationDashboardBatch] Error:', err);\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "data:sector-compare",
      "dom": "data",
      "start": 584,
      "end": 593,
      "lines": [
        "  // ── Sector Comparison (JVS-50) ──────────────────────────────────────────\r",
        "  ipcMain.handle('data:sector-compare', async (_e, stocks: any[], financialData: any) => {\r",
        "    try {\r",
        "      const financialMap = new Map(Object.entries(financialData || {}));\r",
        "      const result = await compareSectorStocks(stocks, financialMap);\r",
        "      return { success: true, result };\r",
        "    } catch (err: any) {\r",
        "      log.error('[SectorComparison] Error:', err);\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "data:sector-compare-multiple",
      "dom": "data",
      "start": 595,
      "end": 604,
      "lines": [
        "  ipcMain.handle('data:sector-compare-multiple', async (_e, sectors: any[], financialData: any) => {\r",
        "    try {\r",
        "      const financialMap = new Map(Object.entries(financialData || {}));\r",
        "      const result = await compareMultipleSectors(sectors, financialMap);\r",
        "      return { success: true, result };\r",
        "    } catch (err: any) {\r",
        "      log.error('[SectorComparisonMultiple] Error:', err);\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "data:sector-rank",
      "dom": "data",
      "start": 606,
      "end": 614,
      "lines": [
        "  ipcMain.handle('data:sector-rank', async (_e, metrics: any[], weights?: any) => {\r",
        "    try {\r",
        "      const result = rankSectorStocks(metrics, weights);\r",
        "      return { success: true, result };\r",
        "    } catch (err: any) {\r",
        "      log.error('[SectorRank] Error:', err);\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "factor:score",
      "dom": "factor",
      "start": 725,
      "end": 733,
      "lines": [
        "  // ── Multi-Factor Selector (JVS-56) ─────────────────────────────────────\r",
        "  ipcMain.handle('factor:score', async (_e, stocks: any[], factorWeights?: any) => {\r",
        "    try {\r",
        "      const result = scoreAndRankStocks(stocks, factorWeights);\r",
        "      return { success: true, result };\r",
        "    } catch (err: any) {\r",
        "      log.error('[MultiFactor] Score error:', err);\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "factor:screen",
      "dom": "factor",
      "start": 735,
      "end": 743,
      "lines": [
        "  ipcMain.handle('factor:screen', async (_e, stocks: any[], criteria: any, factorWeights?: any) => {\r",
        "    try {\r",
        "      const result = screenStocks(stocks, criteria, factorWeights);\r",
        "      return { success: true, result };\r",
        "    } catch (err: any) {\r",
        "      log.error('[MultiFactor] Screen error:', err);\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "factor:screen-batch",
      "dom": "factor",
      "start": 745,
      "end": 753,
      "lines": [
        "  ipcMain.handle('factor:screen-batch', async (_e, batches: any[]) => {\r",
        "    try {\r",
        "      const result = await batchScreenStocks(batches);\r",
        "      return { success: true, result };\r",
        "    } catch (err: any) {\r",
        "      log.error('[MultiFactor] Batch screen error:', err);\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "regime:detect",
      "dom": "regime",
      "start": 1689,
      "end": 1704,
      "lines": [
        "  // ── Q8: Market Regime Detector ─────────────────────────────────────────\r",
        "  ipcMain.handle('regime:detect', async (_e, raw: unknown) => {\r",
        "    try {\r",
        "      const { klines, vixLevel, symbol } = raw as {\r",
        "        klines: { close: number[]; high: number[]; low: number[]; open: number[] };\r",
        "        vixLevel?: number;\r",
        "        symbol?: string;\r",
        "      };\r",
        "      if (!klines || !klines.close || klines.close.length < 30) {\r",
        "        return { success: false, error: 'At least 30 klines required for regime detection' };\r",
        "      }\r",
        "      const result = detectRegime(klines, { vixLevel });\r",
        "      return { success: true, regime: result, symbol };\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "anomaly:detect",
      "dom": "anomaly",
      "start": 1800,
      "end": 1816,
      "lines": [
        "  // ── Q10: Real-time Anomaly Detection ─────────────────────────────────\r",
        "  ipcMain.handle('anomaly:detect', async (_e, raw: unknown) => {\r",
        "    try {\r",
        "      const { values, method, window, threshold } = raw as {\r",
        "        values: number[];\r",
        "        method?: 'zscore' | 'iqr' | 'moving';\r",
        "        window?: number;\r",
        "        threshold?: number;\r",
        "      };\r",
        "      if (!values || values.length < 10) {\r",
        "        return { success: false, error: 'At least 10 data points required' };\r",
        "      }\r",
        "      const result = detectAnomalies({ values, method: method ?? 'zscore', window: window ?? 20, threshold: threshold ?? 3 });\r",
        "      return { success: true, anomalies: result };\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "data:fundamental",
      "dom": "data",
      "start": 2829,
      "end": 2838,
      "lines": [
        "  // ── Data Provider (multi-source integration) ───────────────────────────\r",
        "  ipcMain.handle('data:fundamental', async (_e, symbol: string) => {\r",
        "    if (!dataProvider) return { success: false, error: 'DataProvider not initialized' };\r",
        "    try {\r",
        "      const data = await dataProvider.getFundamental(symbol);\r",
        "      return { success: true, data };\r",
        "    } catch (err: any) {\r",
        "      log.error('[DataProvider] Fundamental fetch failed:', err.message);\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "data:capital-flow",
      "dom": "data",
      "start": 2840,
      "end": 2849,
      "lines": [
        "  ipcMain.handle('data:capital-flow', async (_e, symbol: string) => {\r",
        "    if (!dataProvider) return { success: false, error: 'DataProvider not initialized' };\r",
        "    try {\r",
        "      const data = await dataProvider.getCapitalFlow(symbol);\r",
        "      return { success: true, data };\r",
        "    } catch (err: any) {\r",
        "      log.error('[DataProvider] Capital flow fetch failed:', err.message);\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "data:regime",
      "dom": "data",
      "start": 2851,
      "end": 2860,
      "lines": [
        "  ipcMain.handle('data:regime', async () => {\r",
        "    if (!dataProvider) return { success: false, error: 'DataProvider not initialized' };\r",
        "    try {\r",
        "      const regime = await dataProvider.getMarketRegime();\r",
        "      return { success: true, regime };\r",
        "    } catch (err: any) {\r",
        "      log.error('[DataProvider] Regime fetch failed:', err.message);\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "data:anomalies",
      "dom": "data",
      "start": 2862,
      "end": 2871,
      "lines": [
        "  ipcMain.handle('data:anomalies', async (_e, symbol: string) => {\r",
        "    if (!dataProvider) return { success: false, error: 'DataProvider not initialized' };\r",
        "    try {\r",
        "      const signals = await dataProvider.getAnomalies(symbol);\r",
        "      return { success: true, signals };\r",
        "    } catch (err: any) {\r",
        "      log.error('[DataProvider] Anomalies fetch failed:', err.message);\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "data:news",
      "dom": "data",
      "start": 2873,
      "end": 2882,
      "lines": [
        "  ipcMain.handle('data:news', async (_e, symbol: string, limit?: number) => {\r",
        "    if (!dataProvider) return { success: false, error: 'DataProvider not initialized' };\r",
        "    try {\r",
        "      const items = await dataProvider.getNews(symbol, limit);\r",
        "      return { success: true, items };\r",
        "    } catch (err: any) {\r",
        "      log.error('[DataProvider] News fetch failed:', err.message);\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "data:composite-score",
      "dom": "data",
      "start": 2884,
      "end": 2893,
      "lines": [
        "  ipcMain.handle('data:composite-score', async (_e, symbol: string) => {\r",
        "    if (!dataProvider) return { success: false, error: 'DataProvider not initialized' };\r",
        "    try {\r",
        "      const result = await dataProvider.getCompositeScore(symbol);\r",
        "      return { success: true, result };\r",
        "    } catch (err: any) {\r",
        "      log.error('[DataProvider] Composite score failed:', err.message);\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "data:save-fundamental",
      "dom": "data",
      "start": 2895,
      "end": 2903,
      "lines": [
        "  ipcMain.handle('data:save-fundamental', async (_e, data: any) => {\r",
        "    if (!dataProvider) return { success: false, error: 'DataProvider not initialized' };\r",
        "    try {\r",
        "      dataProvider.saveFundamental(data);\r",
        "      return { success: true };\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "data:save-capital-flow",
      "dom": "data",
      "start": 2905,
      "end": 2913,
      "lines": [
        "  ipcMain.handle('data:save-capital-flow', async (_e, data: any) => {\r",
        "    if (!dataProvider) return { success: false, error: 'DataProvider not initialized' };\r",
        "    try {\r",
        "      dataProvider.saveCapitalFlow(data);\r",
        "      return { success: true };\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "data:save-regime",
      "dom": "data",
      "start": 2915,
      "end": 2923,
      "lines": [
        "  ipcMain.handle('data:save-regime', async (_e, regime: any) => {\r",
        "    if (!dataProvider) return { success: false, error: 'DataProvider not initialized' };\r",
        "    try {\r",
        "      dataProvider.saveMarketRegime(regime);\r",
        "      return { success: true };\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "data:compute-regime",
      "dom": "data",
      "start": 2925,
      "end": 2933,
      "lines": [
        "  ipcMain.handle('data:compute-regime', async (_e, factors: any) => {\r",
        "    if (!dataProvider) return { success: false, error: 'DataProvider not initialized' };\r",
        "    try {\r",
        "      const regime = dataProvider.computeRegime(factors);\r",
        "      return { success: true, regime };\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "data:save-anomaly",
      "dom": "data",
      "start": 2935,
      "end": 2943,
      "lines": [
        "  ipcMain.handle('data:save-anomaly', async (_e, signal: any) => {\r",
        "    if (!dataProvider) return { success: false, error: 'DataProvider not initialized' };\r",
        "    try {\r",
        "      dataProvider.saveAnomaly(signal);\r",
        "      return { success: true };\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "data:save-news",
      "dom": "data",
      "start": 2945,
      "end": 2953,
      "lines": [
        "  ipcMain.handle('data:save-news', async (_e, symbol: string, items: any[]) => {\r",
        "    if (!dataProvider) return { success: false, error: 'DataProvider not initialized' };\r",
        "    try {\r",
        "      dataProvider.saveNews(symbol, items);\r",
        "      return { success: true };\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "data:clear-cache",
      "dom": "data",
      "start": 2955,
      "end": 2963,
      "lines": [
        "  ipcMain.handle('data:clear-cache', async () => {\r",
        "    if (!dataProvider) return { success: false, error: 'DataProvider not initialized' };\r",
        "    try {\r",
        "      dataProvider.clearExpiredCache();\r",
        "      return { success: true };\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "screener:search",
      "dom": "screener",
      "start": 3027,
      "end": 3036,
      "lines": [
        "  // ── Stock Screener (JVS-4) ─────────────────────────────────────────────\r",
        "  ipcMain.handle('screener:search', async (_e, request: any) => {\r",
        "    if (!stockScreener) return { success: false, error: 'StockScreener not initialized' };\r",
        "    try {\r",
        "      const result = await stockScreener.search(request || { query: '' });\r",
        "      return { success: true, ...result };\r",
        "    } catch (err: any) {\r",
        "      log.error('[StockScreener] Search failed:', err.message);\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "data:scheduler-status",
      "dom": "data",
      "start": 3137,
      "end": 3140,
      "lines": [
        "  // ── Data Scheduler (auto-refresh) ─────────────────────────────────────\r",
        "  ipcMain.handle('data:scheduler-status', async () => {\r",
        "    if (!dataScheduler) return { success: false, error: 'DataScheduler not initialized' };\r",
        "    return { success: true, status: dataScheduler.getStatus() };\r",
        "  });\r"
      ]
    },
    {
      "chan": "data:scheduler-refresh",
      "dom": "data",
      "start": 3142,
      "end": 3154,
      "lines": [
        "  ipcMain.handle('data:scheduler-refresh', async (_e, module?: string) => {\r",
        "    if (!dataScheduler) return { success: false, error: 'DataScheduler not initialized' };\r",
        "    try {\r",
        "      if (module) {\r",
        "        await dataScheduler.refreshNow(module);\r",
        "      } else {\r",
        "        await dataScheduler.refreshAll();\r",
        "      }\r",
        "      return { success: true };\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "py:call-skill",
      "dom": "py",
      "start": 3435,
      "end": 3443,
      "lines": [
        "  // ── Python Script Proxy Layer (JVS-20) ──────────────────────────────────\r",
        "  ipcMain.handle('py:call-skill', async (_e, skillName: string, query: string, options?: any) => {\r",
        "    try {\r",
        "      const proxy = getPythonProxy();\r",
        "      const result = await proxy.callSkill(skillName, query, options);\r",
        "      return result;\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "py:list-skills",
      "dom": "py",
      "start": 3445,
      "end": 3452,
      "lines": [
        "  ipcMain.handle('py:list-skills', async () => {\r",
        "    try {\r",
        "      const proxy = getPythonProxy();\r",
        "      return { success: true, skills: proxy.listAvailableSkills() };\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "py:proxy-status",
      "dom": "py",
      "start": 3454,
      "end": 3461,
      "lines": [
        "  ipcMain.handle('py:proxy-status', async () => {\r",
        "    try {\r",
        "      const proxy = getPythonProxy();\r",
        "      return { success: true, status: proxy.getStatus() };\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "data:quality-check",
      "dom": "data",
      "start": 3524,
      "end": 3532,
      "lines": [
        "  // ── Data Quality Monitor (JVS-22) ────────────────────────────────────────\r",
        "  ipcMain.handle('data:quality-check', async () => {\r",
        "    try {\r",
        "      const monitor = getDataQualityMonitor();\r",
        "      const report = await monitor.runHealthCheck();\r",
        "      return { success: true, report };\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "data:quality-report",
      "dom": "data",
      "start": 3534,
      "end": 3542,
      "lines": [
        "  ipcMain.handle('data:quality-report', async () => {\r",
        "    try {\r",
        "      const monitor = getDataQualityMonitor();\r",
        "      const report = monitor.getLastReport();\r",
        "      return { success: true, report };\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "data:quality-acknowledge",
      "dom": "data",
      "start": 3544,
      "end": 3552,
      "lines": [
        "  ipcMain.handle('data:quality-acknowledge', async (_e, alertIndex: number) => {\r",
        "    try {\r",
        "      const monitor = getDataQualityMonitor();\r",
        "      monitor.acknowledgeAlert(alertIndex);\r",
        "      return { success: true };\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "data:quality-clear-acknowledged",
      "dom": "data",
      "start": 3554,
      "end": 3562,
      "lines": [
        "  ipcMain.handle('data:quality-clear-acknowledged', async () => {\r",
        "    try {\r",
        "      const monitor = getDataQualityMonitor();\r",
        "      monitor.clearAcknowledgedAlerts();\r",
        "      return { success: true };\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "data:quality-start-periodic",
      "dom": "data",
      "start": 3564,
      "end": 3572,
      "lines": [
        "  ipcMain.handle('data:quality-start-periodic', async (_e, intervalMs?: number) => {\r",
        "    try {\r",
        "      const monitor = getDataQualityMonitor();\r",
        "      monitor.startPeriodicCheck(intervalMs);\r",
        "      return { success: true };\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "data:quality-stop-periodic",
      "dom": "data",
      "start": 3574,
      "end": 3582,
      "lines": [
        "  ipcMain.handle('data:quality-stop-periodic', async () => {\r",
        "    try {\r",
        "      const monitor = getDataQualityMonitor();\r",
        "      monitor.stopPeriodicCheck();\r",
        "      return { success: true };\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "data:quality-stream-start",
      "dom": "data",
      "start": 3585,
      "end": 3607,
      "lines": [
        "  // ── Data Quality Stream Monitor (JVS-31) ─────────────────────────────────\r",
        "  ipcMain.handle('data:quality-stream-start', async () => {\r",
        "    try {\r",
        "      const monitor = getDataQualityStream();\r",
        "      monitor.start();\r",
        "      \r",
        "      // Forward alerts to renderer\r",
        "      monitor.on('alert', (alert) => {\r",
        "        if (mainWindow && !mainWindow.isDestroyed()) {\r",
        "          mainWindow.webContents.send('data:quality-stream-alert', alert);\r",
        "        }\r",
        "      });\r",
        "      \r",
        "      // Hook into WebSocket stream\r",
        "      const wsStream = getWsDataStream();\r",
        "      wsStream.on('tick', (tick) => {\r",
        "        monitor.validateTick(tick);\r",
        "      });\r",
        "      \r",
        "      return { success: true };\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "data:quality-stream-stop",
      "dom": "data",
      "start": 3609,
      "end": 3617,
      "lines": [
        "  ipcMain.handle('data:quality-stream-stop', async () => {\r",
        "    try {\r",
        "      const monitor = getDataQualityStream();\r",
        "      monitor.stop();\r",
        "      return { success: true };\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "data:quality-stream-status",
      "dom": "data",
      "start": 3619,
      "end": 3629,
      "lines": [
        "  ipcMain.handle('data:quality-stream-status', async () => {\r",
        "    try {\r",
        "      const monitor = getDataQualityStream();\r",
        "      const status = monitor.getStatus();\r",
        "      // Convert Map to object for IPC serialization\r",
        "      const symbolStats = Object.fromEntries(status.symbolStats);\r",
        "      return { success: true, status: { ...status, symbolStats } };\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "data:quality-stream-clear-alerts",
      "dom": "data",
      "start": 3631,
      "end": 3639,
      "lines": [
        "  ipcMain.handle('data:quality-stream-clear-alerts', async () => {\r",
        "    try {\r",
        "      const monitor = getDataQualityStream();\r",
        "      monitor.clearAlerts();\r",
        "      return { success: true };\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "data:quality-stream-reset-metrics",
      "dom": "data",
      "start": 3641,
      "end": 3649,
      "lines": [
        "  ipcMain.handle('data:quality-stream-reset-metrics', async () => {\r",
        "    try {\r",
        "      const monitor = getDataQualityStream();\r",
        "      monitor.resetMetrics();\r",
        "      return { success: true };\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "data:quality-dashboard",
      "dom": "data",
      "start": 3729,
      "end": 3736,
      "lines": [
        "  // ── Data Quality Dashboard Aggregator (JVS-34) ────────────────────────\r",
        "  ipcMain.handle('data:quality-dashboard', async () => {\r",
        "    try {\r",
        "      const dashboard = await getDataQualityDashboard();\r",
        "      return { success: true, dashboard };\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "data:export",
      "dom": "data",
      "start": 3777,
      "end": 3784,
      "lines": [
        "  // ── Data Export Service (JVS-37) ──────────────────────────────────────\r",
        "  ipcMain.handle('data:export', async (_e, request: any) => {\r",
        "    try {\r",
        "      const result = await exportData(request);\r",
        "      return result;\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "data:export-modules",
      "dom": "data",
      "start": 3786,
      "end": 3793,
      "lines": [
        "  ipcMain.handle('data:export-modules', async () => {\r",
        "    try {\r",
        "      const modules = getAvailableModules();\r",
        "      return { success: true, modules };\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "rate-limiter:stats",
      "dom": "rate-limiter",
      "start": 3796,
      "end": 3804,
      "lines": [
        "  // ── Rate Limiter (JVS-38) ─────────────────────────────────────────────\r",
        "  ipcMain.handle('rate-limiter:stats', async (_e, apiName?: string) => {\r",
        "    try {\r",
        "      const manager = getRateLimiterManager();\r",
        "      const stats = manager.getStats(apiName);\r",
        "      return { success: true, stats };\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "rate-limiter:reset",
      "dom": "rate-limiter",
      "start": 3806,
      "end": 3814,
      "lines": [
        "  ipcMain.handle('rate-limiter:reset', async () => {\r",
        "    try {\r",
        "      const manager = getRateLimiterManager();\r",
        "      manager.resetAll();\r",
        "      return { success: true };\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "rate-limiter:apis",
      "dom": "rate-limiter",
      "start": 3816,
      "end": 3824,
      "lines": [
        "  ipcMain.handle('rate-limiter:apis', async () => {\r",
        "    try {\r",
        "      const manager = getRateLimiterManager();\r",
        "      const apis = manager.getAvailableAPIs();\r",
        "      return { success: true, apis };\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "data:consistency-check",
      "dom": "data",
      "start": 3827,
      "end": 3834,
      "lines": [
        "  // ── Data Consistency Checker (JVS-39) ─────────────────────────────────\r",
        "  ipcMain.handle('data:consistency-check', async () => {\r",
        "    try {\r",
        "      const report = await runConsistencyCheck();\r",
        "      return { success: true, report };\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "data:consistency-rules",
      "dom": "data",
      "start": 3836,
      "end": 3843,
      "lines": [
        "  ipcMain.handle('data:consistency-rules', async () => {\r",
        "    try {\r",
        "      const rules = getConsistencyRules();\r",
        "      return { success: true, rules };\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    }
  ],
  "broker": [
    {
      "chan": "order:route",
      "dom": "order",
      "start": 516,
      "end": 524,
      "lines": [
        "  ipcMain.handle('order:route', async (_e, params: any) => {\r",
        "    try {\r",
        "      const result = orderRouter.route(params);\r",
        "      return { success: true, result };\r",
        "    } catch (err: any) {\r",
        "      log.error('[OrderRouter] Error:', err);\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "order:tca",
      "dom": "order",
      "start": 528,
      "end": 536,
      "lines": [
        "  ipcMain.handle('order:tca', async (_e, params: any) => {\r",
        "    try {\r",
        "      const result = tcaEngine.analyze(params);\r",
        "      return { success: true, result };\r",
        "    } catch (err: any) {\r",
        "      log.error('[TCA] Error:', err);\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "pnl:multi-broker",
      "dom": "pnl",
      "start": 540,
      "end": 548,
      "lines": [
        "  ipcMain.handle('pnl:multi-broker', async (_e, params?: any) => {\r",
        "    try {\r",
        "      const result = multiBrokerPnL.consolidate(params);\r",
        "      return { success: true, result };\r",
        "    } catch (err: any) {\r",
        "      log.error('[MultiBrokerPnL] Error:', err);\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "execution:analyze",
      "dom": "execution",
      "start": 797,
      "end": 809,
      "lines": [
        "  // ── Q29/Q54: Execution Analytics ────────────────────────────────────────\r",
        "  ipcMain.handle('execution:analyze', async (_e, raw: unknown) => {\r",
        "    try {\r",
        "      const { executionRecords, marketData, benchmarkPrice, optionsScope } = raw as {\r",
        "        executionRecords: any[]; marketData?: any; benchmarkPrice?: number; optionsScope?: any;\r",
        "      };\r",
        "      const { ExecutionAnalyticsEngine } = await import('./engine/execution-analytics.js');\r",
        "      const engine = new ExecutionAnalyticsEngine();\r",
        "      const result = engine.analyzeExecution(executionRecords, marketData, benchmarkPrice, optionsScope);\r",
        "      return { success: true, result };\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "trader:execute",
      "dom": "trader",
      "start": 885,
      "end": 895,
      "lines": [
        "  // ── Q20: Real Trader ─────────────────────────────────────────────────────\r",
        "  ipcMain.handle('trader:execute', async (_e, raw: unknown) => {\r",
        "    try {\r",
        "      const { signal, paperMode } = raw as { signal: any; paperMode?: boolean };\r",
        "      const { getRealTrader } = await import('./engine/real-trader.js');\r",
        "      const trader = getRealTrader();\r",
        "      const result = await trader.executeSignal(signal, paperMode);\r",
        "      return { success: true, result };\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "trader:get-status",
      "dom": "trader",
      "start": 897,
      "end": 905,
      "lines": [
        "  ipcMain.handle('trader:get-status', async () => {\r",
        "    try {\r",
        "      const { getRealTrader } = await import('./engine/real-trader.js');\r",
        "      const trader = getRealTrader();\r",
        "      return { success: true, status: trader.getStatus(), metrics: trader.getMetrics() };\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "broker:connect",
      "dom": "broker",
      "start": 1080,
      "end": 1128,
      "lines": [
        "  // ── Broker: Multi-broker support (WP1 + Sprint1) ────────────────────\r",
        "  ipcMain.handle('broker:connect', async (_e, config: { host: string; port: number; brokerId?: string }) => {\r",
        "    try {\r",
        "      // Use BrokerManager if available, fallback to legacy opendClient\r",
        "      if (brokerManager) {\r",
        "        const brokerCfg: BrokerConfig = {\r",
        "          id: config.brokerId || 'futu-default',\r",
        "          name: config.brokerId || 'Futu OpenD',\r",
        "          type: 'futu',\r",
        "          host: config.host || '127.0.0.1',\r",
        "          port: config.port || 11111,\r",
        "          enabled: true,\r",
        "        };\r",
        "        brokerManager.loadConfigs([brokerCfg]);\r",
        "        brokerManager.clearCallbacks();\r",
        "        brokerManager.onQuotePush(quotePushHandler);\r",
        "        await brokerManager.connect(brokerCfg.id);\r",
        "        // Load watchlist\r",
        "        const savedWatchlist = db?.getWatchlist();\r",
        "        if (savedWatchlist && savedWatchlist.length > 0) {\r",
        "          WATCHLIST = savedWatchlist;\r",
        "        }\r",
        "        await brokerManager.subscribeAndPush(brokerCfg.id, WATCHLIST);\r",
        "        log.info('[Broker] Multi-broker connected:', brokerCfg.id);\r",
        "        return { success: true, brokerId: brokerCfg.id, host: config.host, port: config.port };\r",
        "      }\r",
        "\r",
        "      // Legacy single-broker path\r",
        "      opendClient = new FutuOpenDClient(config.host || '127.0.0.1', config.port || 11111);\r",
        "      await opendClient.connect();\r",
        "      log.info('[Broker] OpenD connected');\r",
        "\r",
        "      opendClient.onQuotePush((quotes) => {\r",
        "        mainWindow?.webContents.send('quotes:push', quotes);\r",
        "        strategyEngine?.onQuoteUpdate(quotes);\r",
        "      });\r",
        "      const savedWatchlist = db?.getWatchlist();\r",
        "      if (savedWatchlist && savedWatchlist.length > 0) {\r",
        "        WATCHLIST = savedWatchlist;\r",
        "        log.info('[Broker] Loaded watchlist from DB:', WATCHLIST);\r",
        "      }\r",
        "      await opendClient.subscribeAndPush(WATCHLIST);\r",
        "      log.info('[Broker] Push mode active');\r",
        "\r",
        "      return { success: true, host: config.host, port: config.port };\r",
        "    } catch (err: any) {\r",
        "      log.error('[Broker] Connect failed:', err.message);\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "broker:disconnect",
      "dom": "broker",
      "start": 1130,
      "end": 1134,
      "lines": [
        "  ipcMain.handle('broker:disconnect', async () => {\r",
        "    opendClient?.disconnect();\r",
        "    opendClient = null;\r",
        "    return { success: true };\r",
        "  });\r"
      ]
    },
    {
      "chan": "broker:getAccounts",
      "dom": "broker",
      "start": 1136,
      "end": 1141,
      "lines": [
        "  ipcMain.handle('broker:getAccounts', async () => {\r",
        "    if (!opendClient?.connected) return { success: false, error: 'Not connected' };\r",
        "    try {\r",
        "      return { success: true, accounts: await opendClient.getAccounts() };\r",
        "    } catch (err: any) { return { success: false, error: err.message }; }\r",
        "  });\r"
      ]
    },
    {
      "chan": "broker:getFunds",
      "dom": "broker",
      "start": 1143,
      "end": 1150,
      "lines": [
        "  ipcMain.handle('broker:getFunds', async (_e, accountId: string) => {\r",
        "    if (!opendClient?.connected) return { success: false, error: 'Not connected' };\r",
        "    try {\r",
        "      const funds = await opendClient.getFunds(accountId);\r",
        "      riskEngine?.updateTotalAssets(funds?.totalAssets || 0);\r",
        "      return { success: true, funds };\r",
        "    } catch (err: any) { return { success: false, error: err.message }; }\r",
        "  });\r"
      ]
    },
    {
      "chan": "broker:getPositions",
      "dom": "broker",
      "start": 1152,
      "end": 1157,
      "lines": [
        "  ipcMain.handle('broker:getPositions', async (_e, accountId: string) => {\r",
        "    if (!opendClient?.connected) return { success: false, error: 'Not connected' };\r",
        "    try {\r",
        "      return { success: true, positions: await opendClient.getPositions(accountId) };\r",
        "    } catch (err: any) { return { success: false, error: err.message }; }\r",
        "  });\r"
      ]
    },
    {
      "chan": "broker:getQuotes",
      "dom": "broker",
      "start": 1159,
      "end": 1165,
      "lines": [
        "  ipcMain.handle('broker:getQuotes', async (_e, codes: string[]) => {\r",
        "    if (!opendClient?.connected) return { success: false, error: 'Not connected' };\r",
        "    try {\r",
        "      const quoteList = (!codes || codes.length === 0) ? WATCHLIST : codes;\r",
        "      return { success: true, quotes: await opendClient.getQuotes(quoteList) };\r",
        "    } catch (err: any) { return { success: false, error: err.message }; }\r",
        "  });\r"
      ]
    },
    {
      "chan": "broker:subscribe",
      "dom": "broker",
      "start": 1168,
      "end": 1180,
      "lines": [
        "  // ── Subscribe / Unsubscribe (WP1: 动态监控列表) ────────────────────\r",
        "  ipcMain.handle('broker:subscribe', async (_e, codes: string[]) => {\r",
        "    if (!opendClient?.connected) return { success: false, error: 'Not connected' };\r",
        "    try {\r",
        "      // Merge with existing watchlist, dedupe\r",
        "      const merged = Array.from(new Set([...WATCHLIST, ...codes]));\r",
        "      WATCHLIST = merged;\r",
        "      await opendClient.subscribeAndPush(WATCHLIST);\r",
        "      // Persist to DB\r",
        "      db?.saveWatchlist(WATCHLIST);\r",
        "      log.info('[Broker] Subscribed:', codes);\r",
        "      return { success: true, watchlist: WATCHLIST };\r",
        "    } catch (err: any) { return { success: false, error: err.message }; }\r",
        "  });\r"
      ]
    },
    {
      "chan": "broker:unsubscribe",
      "dom": "broker",
      "start": 1182,
      "end": 1191,
      "lines": [
        "  ipcMain.handle('broker:unsubscribe', async (_e, codes: string[]) => {\r",
        "    if (!opendClient?.connected) return { success: false, error: 'Not connected' };\r",
        "    try {\r",
        "      WATCHLIST = WATCHLIST.filter((c) => !codes.includes(c));\r",
        "      await opendClient.subscribeAndPush(WATCHLIST);\r",
        "      db?.saveWatchlist(WATCHLIST);\r",
        "      log.info('[Broker] Unsubscribed:', codes);\r",
        "      return { success: true, watchlist: WATCHLIST };\r",
        "    } catch (err: any) { return { success: false, error: err.message }; }\r",
        "  });\r"
      ]
    },
    {
      "chan": "broker:getKlines",
      "dom": "broker",
      "start": 1193,
      "end": 1208,
      "lines": [
        "  ipcMain.handle('broker:getKlines', async (_e, code: string, period: string, count: number) => {\r",
        "    if (!opendClient?.connected) return { success: false, error: 'Not connected' };\r",
        "    try {\r",
        "      // Check cache first\r",
        "      const cached = db?.getKlines(code, period || 'daily', count || 200);\r",
        "      if (cached && cached.length > 0) {\r",
        "        return { success: true, klines: cached, cached: true };\r",
        "      }\r",
        "      const klines = await opendClient.getKlines(code, period || 'daily', count || 200);\r",
        "      // Cache for future use\r",
        "      if (klines.length > 0 && db) {\r",
        "        db.saveKlines(code, period || 'daily', klines);\r",
        "      }\r",
        "      return { success: true, klines };\r",
        "    } catch (err: any) { return { success: false, error: err.message }; }\r",
        "  });\r"
      ]
    },
    {
      "chan": "broker:placeOrder",
      "dom": "broker",
      "start": 1211,
      "end": 1240,
      "lines": [
        "  // ── Order Placement (with input validation) ─────────────────────────\r",
        "  ipcMain.handle('broker:placeOrder', async (_e, order: any) => {\r",
        "    if (!opendClient?.connected) return { success: false, error: 'Not connected' };\r",
        "    // Input validation\r",
        "    if (!order || typeof order !== 'object') {\r",
        "      return { success: false, error: 'Invalid order object' };\r",
        "    }\r",
        "    if (!order.code || typeof order.code !== 'string') {\r",
        "      return { success: false, error: 'Missing or invalid order.code' };\r",
        "    }\r",
        "    if (!['BUY', 'SELL'].includes(order.side)) {\r",
        "      return { success: false, error: 'Invalid order.side (must be BUY or SELL)' };\r",
        "    }\r",
        "    if (typeof order.qty !== 'number' || order.qty <= 0 || order.qty > 1000000 || !Number.isInteger(order.qty)) {\r",
        "      return { success: false, error: 'Invalid order.qty (must be positive integer <= 1,000,000)' };\r",
        "    }\r",
        "    if (order.price !== undefined && (typeof order.price !== 'number' || order.price < 0)) {\r",
        "      return { success: false, error: 'Invalid order.price' };\r",
        "    }\r",
        "    const riskResult = riskEngine?.checkOrder(order);\r",
        "    if (riskResult && !riskResult.pass) {\r",
        "      mainWindow?.webContents.send('risk-alert', { order, reason: riskResult.reason });\r",
        "      return { success: false, error: `风控拦截: ${riskResult.reason}` };\r",
        "    }\r",
        "    try {\r",
        "      const result = await opendClient.placeOrder(order);\r",
        "      db?.saveTrade({ ...order, orderId: result.orderId, status: 'submitted' });\r",
        "      mainWindow?.webContents.send('order-update', { ...order, orderId: result.orderId, status: 'submitted' });\r",
        "      return { success: true, ...result };\r",
        "    } catch (err: any) { return { success: false, error: err.message }; }\r",
        "  });\r"
      ]
    },
    {
      "chan": "broker:cancelOrder",
      "dom": "broker",
      "start": 1242,
      "end": 1248,
      "lines": [
        "  ipcMain.handle('broker:cancelOrder', async (_e, orderId: string, accountId: string, code: string) => {\r",
        "    if (!opendClient?.connected) return { success: false, error: 'Not connected' };\r",
        "    try {\r",
        "      await opendClient.cancelOrder(orderId, accountId, code);\r",
        "      return { success: true };\r",
        "    } catch (err: any) { return { success: false, error: err.message }; }\r",
        "  });\r"
      ]
    },
    {
      "chan": "broker:getOrders",
      "dom": "broker",
      "start": 1250,
      "end": 1255,
      "lines": [
        "  ipcMain.handle('broker:getOrders', async (_e, accountId: string) => {\r",
        "    if (!opendClient?.connected) return { success: false, error: 'Not connected' };\r",
        "    try {\r",
        "      return { success: true, orders: await opendClient.getOrders(accountId) };\r",
        "    } catch (err: any) { return { success: false, error: err.message }; }\r",
        "  });\r"
      ]
    },
    {
      "chan": "broker:list",
      "dom": "broker",
      "start": 1258,
      "end": 1260,
      "lines": [
        "  // ── Broker Manager (Sprint1: multi-broker) ──────────────────────────\r",
        "  ipcMain.handle('broker:list', async () => {\r",
        "    return { success: true, brokers: brokerManager?.getConfigs() || [] };\r",
        "  });\r"
      ]
    },
    {
      "chan": "broker:add",
      "dom": "broker",
      "start": 1262,
      "end": 1270,
      "lines": [
        "  ipcMain.handle('broker:add', async (_e, cfg: BrokerConfig) => {\r",
        "    const vErr = validate(BrokerAddSchema, { cfg });\r",
        "    if (vErr) return vErr;\r",
        "    try {\r",
        "      brokerManager?.addConfig(cfg);\r",
        "      db?.saveBrokerConfig(cfg);\r",
        "      return { success: true };\r",
        "    } catch (err: any) { return { success: false, error: err.message }; }\r",
        "  });\r"
      ]
    },
    {
      "chan": "broker:remove",
      "dom": "broker",
      "start": 1272,
      "end": 1278,
      "lines": [
        "  ipcMain.handle('broker:remove', async (_e, id: string) => {\r",
        "    try {\r",
        "      brokerManager?.removeConfig(id);\r",
        "      db?.deleteBrokerConfig(id);\r",
        "      return { success: true };\r",
        "    } catch (err: any) { return { success: false, error: err.message }; }\r",
        "  });\r"
      ]
    },
    {
      "chan": "broker:setActive",
      "dom": "broker",
      "start": 1280,
      "end": 1285,
      "lines": [
        "  ipcMain.handle('broker:setActive', async (_e, id: string) => {\r",
        "    try {\r",
        "      brokerManager?.setActiveBroker(id);\r",
        "      return { success: true, activeBroker: id };\r",
        "    } catch (err: any) { return { success: false, error: err.message }; }\r",
        "  });\r"
      ]
    },
    {
      "chan": "broker:switch",
      "dom": "broker",
      "start": 1288,
      "end": 1326,
      "lines": [
        "  // ── Broker Switching (Sprint1) ───────────────────────────────────────\r",
        "  ipcMain.handle('broker:switch', async (_e, id: string) => {\r",
        "    const vErr = validate(BrokerSwitchSchema, { id });\r",
        "    if (vErr) return vErr;\r",
        "    try {\r",
        "      const adapter = brokerManager?.getAdapters().get(id);\r",
        "      if (!adapter) {\r",
        "        // Broker not yet connected — connect first\r",
        "        const config = brokerManager?.getConfigs().find((c: any) => c.id === id);\r",
        "        if (!config) return { success: false, error: `Broker config not found: ${id}` };\r",
        "\r",
        "        brokerManager?.loadConfigs([config]);\r",
        "        await brokerManager?.connect(id);\r",
        "      } else if (!adapter.connected) {\r",
        "        await adapter.connect();\r",
        "      }\r",
        "\r",
        "      brokerManager?.setActiveBroker(id);\r",
        "      const activeId = brokerManager?.getActiveBrokerId();\r",
        "      const activeAdapter = brokerManager?.getActiveBroker();\r",
        "\r",
        "      // Re-subscribe quotes for the newly active broker\r",
        "      if (activeAdapter) {\r",
        "        brokerManager?.clearCallbacks();\r",
        "        brokerManager?.onQuotePush(quotePushHandler);\r",
        "        const savedWatchlist = db?.getWatchlist();\r",
        "        await activeAdapter.subscribeAndPush(savedWatchlist && savedWatchlist.length > 0 ? savedWatchlist : WATCHLIST);\r",
        "      }\r",
        "\r",
        "      const status = brokerManager?.getStatus() || [];\r",
        "      const switched = status.find((s: any) => s.id === activeId);\r",
        "\r",
        "      log.info(`[Broker] Switched to ${id}, connected=${switched?.connected}`);\r",
        "      mainWindow?.webContents.send('broker:switched', { activeBroker: activeId, status });\r",
        "      return { success: true, activeBroker: activeId, brokerStatus: switched };\r",
        "    } catch (err: any) {\r",
        "      log.error('[Broker] Switch failed:', err.message);\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "broker:getStatus",
      "dom": "broker",
      "start": 1328,
      "end": 1330,
      "lines": [
        "  ipcMain.handle('broker:getStatus', async () => {\r",
        "    return { success: true, status: brokerManager?.getStatus() || [] };\r",
        "  });\r"
      ]
    },
    {
      "chan": "signal:quality-score",
      "dom": "signal",
      "start": 1728,
      "end": 1743,
      "lines": [
        "  // ── Q63: Signal Quality Scorer ───────────────────────────────────────\r",
        "  ipcMain.handle('signal:quality-score', async (_e, raw: unknown) => {\r",
        "    try {\r",
        "      const { signalType, marketContext, backtestHistory, signalParams } = raw as {\r",
        "        signalType: string;\r",
        "        marketContext?: any;\r",
        "        backtestHistory?: any[];\r",
        "        signalParams?: any;\r",
        "      };\r",
        "      const { SignalQualityScorer } = await import('./engine/signal-quality-scorer.js');\r",
        "      const scorer = new SignalQualityScorer();\r",
        "      const report = scorer.score(signalType, marketContext, backtestHistory, signalParams);\r",
        "      return { success: true, report };\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "position:check",
      "dom": "position",
      "start": 1746,
      "end": 1762,
      "lines": [
        "  // ── Q68: Position Alert Engine ───────────────────────────────────────\r",
        "  ipcMain.handle('position:check', async (_e, raw: unknown) => {\r",
        "    try {\r",
        "      const { positions, accountFunds, config } = raw as {\r",
        "        positions: any[];\r",
        "        accountFunds: number;\r",
        "        config?: any;\r",
        "      };\r",
        "      const { PositionAlertEngine } = await import('./engine/position-alert-engine.js');\r",
        "      const engine = new PositionAlertEngine();\r",
        "      if (config) engine.updateConfig(config);\r",
        "      const alerts = positions.map(pos => engine.checkPosition(pos, accountFunds));\r",
        "      const summary = engine.getSummary();\r",
        "      return { success: true, alerts, summary };\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    }
  ],
  "risk": [
    {
      "chan": "risk:dashboard",
      "dom": "risk",
      "start": 552,
      "end": 560,
      "lines": [
        "  ipcMain.handle('risk:dashboard', async (_e, params?: any) => {\r",
        "    try {\r",
        "      const result = unifiedRiskDash.generate(params);\r",
        "      return { success: true, result };\r",
        "    } catch (err: any) {\r",
        "      log.error('[RiskDashboard] Error:', err);\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "risk:cross-asset",
      "dom": "risk",
      "start": 870,
      "end": 882,
      "lines": [
        "  // ── Q57: Cross-Asset Risk ───────────────────────────────────────────────\r",
        "  ipcMain.handle('risk:cross-asset', async (_e, raw: unknown) => {\r",
        "    try {\r",
        "      const { portfolios, confidenceLevel, method } = raw as {\r",
        "        portfolios: any[]; confidenceLevel?: number; method?: string;\r",
        "      };\r",
        "      const { CrossAssetRiskEngine } = await import('./engine/cross-asset-risk.js');\r",
        "      const engine = new CrossAssetRiskEngine();\r",
        "      const result = engine.calculatePortfolioVaR(portfolios, confidenceLevel, method);\r",
        "      return { success: true, result };\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "risk:decompose",
      "dom": "risk",
      "start": 1765,
      "end": 1780,
      "lines": [
        "  // ── Q9: Strategy Risk Decomposition ──────────────────────────────────\r",
        "  ipcMain.handle('risk:decompose', async (_e, raw: unknown) => {\r",
        "    try {\r",
        "      const { equityCurve, positions, confidenceLevel } = raw as {\r",
        "        equityCurve: number[];\r",
        "        positions?: any[];\r",
        "        confidenceLevel?: number;\r",
        "      };\r",
        "      if (!equityCurve || equityCurve.length < 20) {\r",
        "        return { success: false, error: 'At least 20 equity curve data points required' };\r",
        "      }\r",
        "      const result = decomposeRisk(equityCurve, positions, confidenceLevel ?? 0.95);\r",
        "      return { success: true, decomposition: result };\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "risk:monteCarlo",
      "dom": "risk",
      "start": 1782,
      "end": 1797,
      "lines": [
        "  ipcMain.handle('risk:monteCarlo', async (_e, raw: unknown) => {\r",
        "    try {\r",
        "      const { equityCurve, paths, horizon } = raw as {\r",
        "        equityCurve: number[];\r",
        "        paths?: number;\r",
        "        horizon?: number;\r",
        "      };\r",
        "      if (!equityCurve || equityCurve.length < 20) {\r",
        "        return { success: false, error: 'At least 20 data points required' };\r",
        "      }\r",
        "      const result = runMonteCarlo(equityCurve, paths ?? 10000, horizon ?? 252);\r",
        "      return { success: true, simulation: result };\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "risk:position-size",
      "dom": "risk",
      "start": 2272,
      "end": 2414,
      "lines": [
        "  // ── Q16: Dynamic Position Sizer ────────────────────────────────\r",
        "  ipcMain.handle('risk:position-size', async (_e, raw: unknown) => {\r",
        "    try {\r",
        "      const { calcPositionSize, calcQuickSize, calcPortfolioSizes } = require('./engine/dynamic-sizer');\r",
        "      const req = raw as any;\r",
        "      if (req.strategies != null) {\r",
        "        // Portfolio mode\r",
        "        const result = await calcPortfolioSizes(req);\r",
        "        return { success: true, ...result };\r",
        "      }\r",
        "      if (req.equity == null || req.winRate == null || req.avgWin == null || req.avgLoss == null) {\r",
        "        return { success: false, error: 'equity, winRate, avgWin, avgLoss required' };\r",
        "  // ── Q16: Dynamic Position Sizer ──────────────────────\r",
        "\r",
        "  ipcMain.handle('risk:calculate-size', async (_e, raw: unknown) => {\r",
        "\r",
        "    try {\r",
        "\r",
        "      const { getDynamicSizer } = require('./engine/dynamic-sizer');\r",
        "\r",
        "      const sizer = getDynamicSizer();\r",
        "\r",
        "      if (!sizer) {\r",
        "\r",
        "        return { success: false, error: 'DynamicSizer not initialized' };\r",
        "\r",
        "      }\r",
        "\r",
        "      const req = raw as any;\r",
        "\r",
        "      const result = await sizer.calculateSize(req);\r",
        "\r",
        "      return { success: true, ...result };\r",
        "\r",
        "    } catch (err: any) {\r",
        "\r",
        "      return { success: false, error: err.message };\r",
        "\r",
        "    }\r",
        "\r",
        "  });\r",
        "\r",
        "\r",
        "\r",
        "  ipcMain.handle('risk:calculate-portfolio-sizes', async (_e, raw: unknown) => {\r",
        "\r",
        "    try {\r",
        "\r",
        "      const { getDynamicSizer } = require('./engine/dynamic-sizer');\r",
        "\r",
        "      const sizer = getDynamicSizer();\r",
        "\r",
        "      if (!sizer) {\r",
        "\r",
        "        return { success: false, error: 'DynamicSizer not initialized' };\r",
        "\r",
        "      }\r",
        "\r",
        "      const req = raw as any;\r",
        "\r",
        "      const result = await sizer.calculatePortfolioSizes(req);\r",
        "\r",
        "      return { success: true, ...result };\r",
        "\r",
        "    } catch (err: any) {\r",
        "\r",
        "      return { success: false, error: err.message };\r",
        "\r",
        "    }\r",
        "\r",
        "  });\r",
        "\r",
        "\r",
        "\r",
        "  ipcMain.handle('risk:record-trade', async (_e, raw: unknown) => {\r",
        "\r",
        "    try {\r",
        "\r",
        "      const { getDynamicSizer } = require('./engine/dynamic-sizer');\r",
        "\r",
        "      const sizer = getDynamicSizer();\r",
        "\r",
        "      if (!sizer) {\r",
        "\r",
        "        return { success: false, error: 'DynamicSizer not initialized' };\r",
        "\r",
        "      }\r",
        "\r",
        "      const trade = raw as any;\r",
        "\r",
        "      sizer.recordTrade(trade);\r",
        "\r",
        "      return { success: true };\r",
        "\r",
        "    } catch (err: any) {\r",
        "\r",
        "      return { success: false, error: err.message };\r",
        "\r",
        "    }\r",
        "\r",
        "  });\r",
        "\r",
        "\r",
        "\r",
        "  ipcMain.handle('risk:get-trade-history', async (_e, strategyId?: string) => {\r",
        "\r",
        "    try {\r",
        "\r",
        "      const { getDynamicSizer } = require('./engine/dynamic-sizer');\r",
        "\r",
        "      const sizer = getDynamicSizer();\r",
        "\r",
        "      if (!sizer) {\r",
        "\r",
        "        return { success: false, error: 'DynamicSizer not initialized' };\r",
        "\r",
        "      }\r",
        "\r",
        "      const history = sizer.getTradeHistory(strategyId);\r",
        "\r",
        "      const winRate = sizer.getWinRate(strategyId);\r",
        "\r",
        "      return { success: true, history, winRate };\r",
        "\r",
        "    } catch (err: any) {\r",
        "\r",
        "      return { success: false, error: err.message };\r",
        "\r",
        "    }\r",
        "\r",
        "  });\r",
        "\r",
        "\r",
        "\r",
        "      }\r",
        "      if (req.quick != null) {\r",
        "        return { success: true, ...calcQuickSize(req) };\r",
        "      }\r",
        "      const result = await calcPositionSize(req);\r",
        "      return { success: true, ...result };\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "risk:calculate-size",
      "dom": "risk",
      "start": 2285,
      "end": 2311,
      "lines": [
        "  ipcMain.handle('risk:calculate-size', async (_e, raw: unknown) => {\r",
        "\r",
        "    try {\r",
        "\r",
        "      const { getDynamicSizer } = require('./engine/dynamic-sizer');\r",
        "\r",
        "      const sizer = getDynamicSizer();\r",
        "\r",
        "      if (!sizer) {\r",
        "\r",
        "        return { success: false, error: 'DynamicSizer not initialized' };\r",
        "\r",
        "      }\r",
        "\r",
        "      const req = raw as any;\r",
        "\r",
        "      const result = await sizer.calculateSize(req);\r",
        "\r",
        "      return { success: true, ...result };\r",
        "\r",
        "    } catch (err: any) {\r",
        "\r",
        "      return { success: false, error: err.message };\r",
        "\r",
        "    }\r",
        "\r",
        "  });\r"
      ]
    },
    {
      "chan": "risk:calculate-portfolio-sizes",
      "dom": "risk",
      "start": 2315,
      "end": 2341,
      "lines": [
        "  ipcMain.handle('risk:calculate-portfolio-sizes', async (_e, raw: unknown) => {\r",
        "\r",
        "    try {\r",
        "\r",
        "      const { getDynamicSizer } = require('./engine/dynamic-sizer');\r",
        "\r",
        "      const sizer = getDynamicSizer();\r",
        "\r",
        "      if (!sizer) {\r",
        "\r",
        "        return { success: false, error: 'DynamicSizer not initialized' };\r",
        "\r",
        "      }\r",
        "\r",
        "      const req = raw as any;\r",
        "\r",
        "      const result = await sizer.calculatePortfolioSizes(req);\r",
        "\r",
        "      return { success: true, ...result };\r",
        "\r",
        "    } catch (err: any) {\r",
        "\r",
        "      return { success: false, error: err.message };\r",
        "\r",
        "    }\r",
        "\r",
        "  });\r"
      ]
    },
    {
      "chan": "risk:record-trade",
      "dom": "risk",
      "start": 2345,
      "end": 2371,
      "lines": [
        "  ipcMain.handle('risk:record-trade', async (_e, raw: unknown) => {\r",
        "\r",
        "    try {\r",
        "\r",
        "      const { getDynamicSizer } = require('./engine/dynamic-sizer');\r",
        "\r",
        "      const sizer = getDynamicSizer();\r",
        "\r",
        "      if (!sizer) {\r",
        "\r",
        "        return { success: false, error: 'DynamicSizer not initialized' };\r",
        "\r",
        "      }\r",
        "\r",
        "      const trade = raw as any;\r",
        "\r",
        "      sizer.recordTrade(trade);\r",
        "\r",
        "      return { success: true };\r",
        "\r",
        "    } catch (err: any) {\r",
        "\r",
        "      return { success: false, error: err.message };\r",
        "\r",
        "    }\r",
        "\r",
        "  });\r"
      ]
    },
    {
      "chan": "risk:get-trade-history",
      "dom": "risk",
      "start": 2375,
      "end": 2401,
      "lines": [
        "  ipcMain.handle('risk:get-trade-history', async (_e, strategyId?: string) => {\r",
        "\r",
        "    try {\r",
        "\r",
        "      const { getDynamicSizer } = require('./engine/dynamic-sizer');\r",
        "\r",
        "      const sizer = getDynamicSizer();\r",
        "\r",
        "      if (!sizer) {\r",
        "\r",
        "        return { success: false, error: 'DynamicSizer not initialized' };\r",
        "\r",
        "      }\r",
        "\r",
        "      const history = sizer.getTradeHistory(strategyId);\r",
        "\r",
        "      const winRate = sizer.getWinRate(strategyId);\r",
        "\r",
        "      return { success: true, history, winRate };\r",
        "\r",
        "    } catch (err: any) {\r",
        "\r",
        "      return { success: false, error: err.message };\r",
        "\r",
        "    }\r",
        "\r",
        "  });\r"
      ]
    },
    {
      "chan": "risk:stress-test",
      "dom": "risk",
      "start": 2437,
      "end": 2463,
      "lines": [
        "  // ── Q12: Stress Tester ────────────────────────────────────────────\r",
        "  ipcMain.handle('risk:stress-test', async (_e, raw: unknown) => {\r",
        "    try {\r",
        "      const { runStressTest, runCustomShock, HISTORICAL_SCENARIOS } = require('./engine/stress-tester');\r",
        "      const { positions, scenarioName, customFactors, portfolio } = raw as {\r",
        "        positions: any[];\r",
        "        scenarioName?: string;\r",
        "        customFactors?: any[];\r",
        "        portfolio?: { totalValue: number; dailyVol: number };\r",
        "      };\r",
        "      if (!positions || positions.length === 0) {\r",
        "        return { success: false, error: 'At least one position required' };\r",
        "      }\r",
        "      let result;\r",
        "      if (customFactors) {\r",
        "        result = runCustomShock(positions, customFactors, portfolio ?? { totalValue: 0, dailyVol: 0.02 });\r",
        "      } else {\r",
        "        const scenario = HISTORICAL_SCENARIOS.find((s: any) => s.name === scenarioName);\r",
        "        if (!scenario) {\r",
        "          return { success: false, error: 'Scenario not found: ' + scenarioName + '. Available: ' + HISTORICAL_SCENARIOS.map((s: any) => s.name).join(', ') };\r",
        "        }\r",
        "        result = runStressTest(positions, scenario);\r",
        "      }\r",
        "      return { success: true, ...result };\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "risk:getConfig",
      "dom": "risk",
      "start": 2491,
      "end": 2493,
      "lines": [
        "  // ── Risk Engine ─────────────────────────────────────────────────────\r",
        "  ipcMain.handle('risk:getConfig', async () => {\r",
        "    return { success: true, config: riskEngine?.getConfig() };\r",
        "  });\r"
      ]
    },
    {
      "chan": "risk:updateConfig",
      "dom": "risk",
      "start": 2495,
      "end": 2498,
      "lines": [
        "  ipcMain.handle('risk:updateConfig', async (_e, config: any) => {\r",
        "    riskEngine?.updateConfig(config);\r",
        "    return { success: true };\r",
        "  });\r"
      ]
    },
    {
      "chan": "risk:getAlerts",
      "dom": "risk",
      "start": 2500,
      "end": 2502,
      "lines": [
        "  ipcMain.handle('risk:getAlerts', async () => {\r",
        "    return { success: true, alerts: riskEngine?.getAlerts() || [] };\r",
        "  });\r"
      ]
    },
    {
      "chan": "risk:getStatusSnapshot",
      "dom": "risk",
      "start": 2505,
      "end": 2508,
      "lines": [
        "  // v2: Risk engine status snapshot (for risk dashboard UI)\r",
        "  ipcMain.handle('risk:getStatusSnapshot', async () => {\r",
        "    if (!riskEngine) return { success: false, error: 'RiskEngine not initialized' };\r",
        "    return { success: true, snapshot: riskEngine.getStatusSnapshot() };\r",
        "  });\r"
      ]
    },
    {
      "chan": "risk:getKellyStats",
      "dom": "risk",
      "start": 2510,
      "end": 2513,
      "lines": [
        "  ipcMain.handle('risk:getKellyStats', async () => {\r",
        "    if (!riskEngine) return { success: false, error: 'RiskEngine not initialized' };\r",
        "    return { success: true, kelly: riskEngine.getKellyStats() };\r",
        "  });\r"
      ]
    },
    {
      "chan": "risk:getDrawdownState",
      "dom": "risk",
      "start": 2515,
      "end": 2518,
      "lines": [
        "  ipcMain.handle('risk:getDrawdownState', async () => {\r",
        "    if (!riskEngine) return { success: false, error: 'RiskEngine not initialized' };\r",
        "    return { success: true, drawdown: riskEngine.getDrawdownState() };\r",
        "  });\r"
      ]
    },
    {
      "chan": "risk:updateVix",
      "dom": "risk",
      "start": 2520,
      "end": 2524,
      "lines": [
        "  ipcMain.handle('risk:updateVix', async (_e, vix: number) => {\r",
        "    if (!riskEngine) return { success: false, error: 'RiskEngine not initialized' };\r",
        "    riskEngine.updateVix(vix);\r",
        "    return { success: true };\r",
        "  });\r"
      ]
    }
  ],
  "alert-notification": [
    {
      "chan": "alert:macro",
      "dom": "alert",
      "start": 617,
      "end": 626,
      "lines": [
        "  // ── Macro Alert (JVS-51) ────────────────────────────────────────────────\r",
        "  ipcMain.handle('alert:macro', async (_e, currentData: any[], historicalData: any) => {\r",
        "    try {\r",
        "      const historicalMap = new Map(Object.entries(historicalData || {}) as [string, number[]][]);\r",
        "      const result = await detectMacroAnomalies(currentData, historicalMap);\r",
        "      return { success: true, result };\r",
        "    } catch (err: any) {\r",
        "      log.error('[MacroAlert] Error:', err);\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "alert:macro-multiple",
      "dom": "alert",
      "start": 628,
      "end": 636,
      "lines": [
        "  ipcMain.handle('alert:macro-multiple', async (_e, indicatorData: any[]) => {\r",
        "    try {\r",
        "      const result = await analyzeMultipleIndicators(indicatorData);\r",
        "      return { success: true, result };\r",
        "    } catch (err: any) {\r",
        "      log.error('[MacroAlertMultiple] Error:', err);\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "alert:correlation",
      "dom": "alert",
      "start": 639,
      "end": 648,
      "lines": [
        "  // ── Correlation Alert (JVS-52) ─────────────────────────────────────────\r",
        "  ipcMain.handle('alert:correlation', async (_e, snapshots: any[], historicalData: any) => {\r",
        "    try {\r",
        "      const histMap = new Map(Object.entries(historicalData || {}) as [string, number[]][]);\r",
        "      const result = await detectCorrelationAnomalies(snapshots, histMap);\r",
        "      return { success: true, result };\r",
        "    } catch (err: any) {\r",
        "      log.error('[CorrelationAlert] Error:', err);\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "alert:correlation-matrix",
      "dom": "alert",
      "start": 650,
      "end": 659,
      "lines": [
        "  ipcMain.handle('alert:correlation-matrix', async (_e, matrix: number[][], codes: string[], prevMatrix?: number[][], histMatrices?: any) => {\r",
        "    try {\r",
        "      const histMap = histMatrices ? new Map(Object.entries(histMatrices) as [string, number[]][]) : undefined;\r",
        "      const result = await analyzeCorrelationMatrix(matrix, codes, prevMatrix, histMap);\r",
        "      return { success: true, result };\r",
        "    } catch (err: any) {\r",
        "      log.error('[CorrelationAlertMatrix] Error:', err);\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "notification:generate",
      "dom": "notification",
      "start": 1632,
      "end": 1638,
      "lines": [
        "  // ── Smart Notification Engine ───────────────────────────────────────\r",
        "  ipcMain.handle('notification:generate', async (_e, raw: unknown) => {\r",
        "    const vErr = validate(NotificationGenerateSchema, raw);\r",
        "    if (vErr) return vErr;\r",
        "    const ctx = raw as NotificationContext;\r",
        "    const alerts = generateSmartAlerts(ctx);\r",
        "    return { success: true, alerts };\r",
        "  });\r"
      ]
    },
    {
      "chan": "notification:summary",
      "dom": "notification",
      "start": 1640,
      "end": 1646,
      "lines": [
        "  ipcMain.handle('notification:summary', async (_e, alerts: SmartAlert[], apiKey?: string) => {\r",
        "    if (!Array.isArray(alerts) || alerts.length === 0) {\r",
        "      return { success: true, summary: '暂无活跃警报。' };\r",
        "    }\r",
        "    const summary = await generateAlertSummary(alerts, apiKey ?? '');\r",
        "    return { success: true, summary };\r",
        "  });\r"
      ]
    }
  ],
  "report": [
    {
      "chan": "report:walk-forward",
      "dom": "report",
      "start": 662,
      "end": 670,
      "lines": [
        "  // ── Walk-Forward Report (JVS-53) ───────────────────────────────────────\r",
        "  ipcMain.handle('report:walk-forward', async (_e, strategyName: string, windows: any[]) => {\r",
        "    try {\r",
        "      const result = generateWalkForwardReport(strategyName, windows);\r",
        "      return { success: true, result };\r",
        "    } catch (err: any) {\r",
        "      log.error('[WalkForwardReport] Error:', err);\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "report:walk-forward-batch",
      "dom": "report",
      "start": 672,
      "end": 680,
      "lines": [
        "  ipcMain.handle('report:walk-forward-batch', async (_e, strategies: any[]) => {\r",
        "    try {\r",
        "      const result = await generateBatchWalkForwardReport(strategies);\r",
        "      return { success: true, result };\r",
        "    } catch (err: any) {\r",
        "      log.error('[WalkForwardReportBatch] Error:', err);\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "report:brinson-attribution",
      "dom": "report",
      "start": 683,
      "end": 691,
      "lines": [
        "  // ── Brinson Attribution (JVS-54) ───────────────────────────────────────\r",
        "  ipcMain.handle('report:brinson-attribution', async (_e, holdings: any[], benchmark: any[], benchmarkReturn: number) => {\r",
        "    try {\r",
        "      const result = generateBrinsonReport(holdings, benchmark, benchmarkReturn);\r",
        "      return { success: true, result };\r",
        "    } catch (err: any) {\r",
        "      log.error('[BrinsonAttribution] Error:', err);\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "report:brinson-batch",
      "dom": "report",
      "start": 693,
      "end": 701,
      "lines": [
        "  ipcMain.handle('report:brinson-batch', async (_e, portfolios: any[]) => {\r",
        "    try {\r",
        "      const result = await generateBatchBrinsonReport(portfolios);\r",
        "      return { success: true, result };\r",
        "    } catch (err: any) {\r",
        "      log.error('[BrinsonAttributionBatch] Error:', err);\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "report:generate",
      "dom": "report",
      "start": 1649,
      "end": 1660,
      "lines": [
        "  // ── AI Report Generator ──────────────────────────────────────────────\r",
        "  ipcMain.handle('report:generate', async (_e, raw: unknown) => {\r",
        "    const vErr = validate(ReportGenerateSchema, raw);\r",
        "    if (vErr) return vErr;\r",
        "    const { results, symbol, apiKey, timeoutMs } = raw as {\r",
        "      results: any[];\r",
        "      symbol?: string;\r",
        "      apiKey?: string;\r",
        "      timeoutMs?: number;\r",
        "    };\r",
        "    const report = await generateBacktestReport(results, symbol, apiKey, timeoutMs ?? 20000);\r",
        "    return { success: true, report };\r",
        "  });\r"
      ]
    },
    {
      "chan": "report:quick",
      "dom": "report",
      "start": 1662,
      "end": 1668,
      "lines": [
        "  ipcMain.handle('report:quick', async (_e, raw: unknown) => {\r",
        "    const vErr = validate(ReportQuickSchema, raw);\r",
        "    if (vErr) return vErr;\r",
        "    const { result, apiKey } = raw as { result: any; apiKey?: string };\r",
        "    const report = await generateQuickReport(result, apiKey);\r",
        "    return { success: true, report };\r",
        "  });\r"
      ]
    }
  ],
  "options": [
    {
      "chan": "options:chain-analyze",
      "dom": "options",
      "start": 704,
      "end": 712,
      "lines": [
        "  // ── Options Chain Analyzer (JVS-55) ─────────────────────────────────\r",
        "  ipcMain.handle('options:chain-analyze', async (_e, contracts: any[], symbol: string, historicalIVRange?: any) => {\r",
        "    try {\r",
        "      const result = analyzeOptionsChain(contracts, symbol, historicalIVRange);\r",
        "      return { success: true, result };\r",
        "    } catch (err: any) {\r",
        "      log.error('[OptionsChain] Error:', err);\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "options:chain-batch",
      "dom": "options",
      "start": 714,
      "end": 722,
      "lines": [
        "  ipcMain.handle('options:chain-batch', async (_e, symbols: any[]) => {\r",
        "    try {\r",
        "      const result = await analyzeBatchOptionsChain(symbols);\r",
        "      return { success: true, result };\r",
        "    } catch (err: any) {\r",
        "      log.error('[OptionsChainBatch] Error:', err);\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "options:build",
      "dom": "options",
      "start": 812,
      "end": 824,
      "lines": [
        "  // ── Q55: Options Strategy Builder ────────────────────────────────────────\r",
        "  ipcMain.handle('options:build', async (_e, raw: unknown) => {\r",
        "    try {\r",
        "      const { underlying, spotPrice, strategyType, targetParams, legs } = raw as {\r",
        "        underlying: string; spotPrice: number; strategyType?: string; targetParams?: any; legs?: any[];\r",
        "      };\r",
        "      const { OptionsStrategyBuilder } = await import('./engine/options-strategy-builder.js');\r",
        "      const builder = new OptionsStrategyBuilder(underlying, spotPrice);\r",
        "      const result = builder.buildStrategy(strategyType, targetParams, legs);\r",
        "      return { success: true, result };\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "options:analyze",
      "dom": "options",
      "start": 826,
      "end": 838,
      "lines": [
        "  ipcMain.handle('options:analyze', async (_e, raw: unknown) => {\r",
        "    try {\r",
        "      const { strategy, spotPrice, volatility, riskFreeRate, dividends } = raw as {\r",
        "        strategy: any; spotPrice: number; volatility?: number; riskFreeRate?: number; dividends?: any;\r",
        "      };\r",
        "      const { OptionsStrategyBuilder } = await import('./engine/options-strategy-builder.js');\r",
        "      const builder = new OptionsStrategyBuilder(strategy.underlying || 'UNKNOWN', spotPrice);\r",
        "      const result = builder.analyzeStrategy(strategy, { spotPrice, volatility, riskFreeRate, dividends });\r",
        "      return { success: true, result };\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "greeks:calculate",
      "dom": "greeks",
      "start": 2682,
      "end": 2693,
      "lines": [
        "  // ── Greeks Calculation (P0-fixed: pure JS Black-Scholes, no Python subprocess) ─\r",
        "  ipcMain.handle('greeks:calculate', async (_e, params: {\r",
        "    spot: number; strike: number; vol: number; days: number;\r",
        "    rate?: number; type: 'CALL' | 'PUT'; qty?: number;\r",
        "  }) => {\r",
        "    try {\r",
        "      const greeks = calcGreeksJS(params.spot, params.strike, params.vol, params.days, params.rate || 0.05, params.type);\r",
        "      return { success: true, greeks };\r",
        "    } catch (err: any) {\r",
        "      log.error('[Greeks] Calculation failed:', err.message);\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "greeks:portfolio",
      "dom": "greeks",
      "start": 2695,
      "end": 2724,
      "lines": [
        "  ipcMain.handle('greeks:portfolio', async (_e, positions: any[]) => {\r",
        "    const vErr = validate(GreeksPortfolioSchema, { positions });\r",
        "    if (vErr) return vErr;\r",
        "    try {\r",
        "      const portfolio = positions.map((p: any) => {\r",
        "        const g = calcGreeksJS(p.spot, p.strike, p.iv, p.dte, p.rate || 0.05, p.type);\r",
        "        const mult = p.qty || 1;\r",
        "        return {\r",
        "          ...g,\r",
        "          symbol: p.symbol,\r",
        "          type: p.type,\r",
        "          strike: p.strike,\r",
        "          qty: mult,\r",
        "          totalDelta: (g.delta * mult * 100).toFixed(2),\r",
        "          totalGamma: (g.gamma * mult * 100).toFixed(4),\r",
        "          totalTheta: (g.theta * mult).toFixed(2),\r",
        "          totalVega: (g.vega * mult * 0.01).toFixed(2),\r",
        "        };\r",
        "      });\r",
        "      const totals = {\r",
        "        netDelta: portfolio.reduce((s: number, p: any) => s + parseFloat(p.totalDelta), 0).toFixed(2),\r",
        "        netGamma: portfolio.reduce((s: number, p: any) => s + parseFloat(p.totalGamma), 0).toFixed(4),\r",
        "        netTheta: portfolio.reduce((s: number, p: any) => s + parseFloat(p.totalTheta), 0).toFixed(2),\r",
        "      };\r",
        "      return { success: true, portfolio: { positions: portfolio, totals } };\r",
        "    } catch (err: any) {\r",
        "      log.error('[Greeks] Portfolio calc failed:', err.message);\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    }
  ],
  "portfolio": [
    {
      "chan": "portfolio:optimize",
      "dom": "portfolio",
      "start": 756,
      "end": 764,
      "lines": [
        "  // ── Portfolio Optimizer (JVS-57) ────────────────────────────────────────\r",
        "  ipcMain.handle('portfolio:optimize', async (_e, assets: any[], constraints?: any) => {\r",
        "    try {\r",
        "      const result = optimizePortfolio(assets, constraints);\r",
        "      return { success: true, result };\r",
        "    } catch (err: any) {\r",
        "      log.error('[PortfolioOptimizer] Error:', err);\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "portfolio:efficient-frontier",
      "dom": "portfolio",
      "start": 766,
      "end": 774,
      "lines": [
        "  ipcMain.handle('portfolio:efficient-frontier', async (_e, assets: any[], points?: number, constraints?: any) => {\r",
        "    try {\r",
        "      const result = generateEfficientFrontier(assets, points, constraints);\r",
        "      return { success: true, result };\r",
        "    } catch (err: any) {\r",
        "      log.error('[PortfolioOptimizer] Frontier error:', err);\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "portfolio:risk-parity",
      "dom": "portfolio",
      "start": 776,
      "end": 784,
      "lines": [
        "  ipcMain.handle('portfolio:risk-parity', async (_e, assets: any[], constraints?: any) => {\r",
        "    try {\r",
        "      const result = riskParityPortfolio(assets, constraints);\r",
        "      return { success: true, result };\r",
        "    } catch (err: any) {\r",
        "      log.error('[PortfolioOptimizer] Risk parity error:', err);\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "portfolio:optimize-batch",
      "dom": "portfolio",
      "start": 786,
      "end": 794,
      "lines": [
        "  ipcMain.handle('portfolio:optimize-batch', async (_e, scenarios: any[]) => {\r",
        "    try {\r",
        "      const result = await batchOptimizePortfolios(scenarios);\r",
        "      return { success: true, result };\r",
        "    } catch (err: any) {\r",
        "      log.error('[PortfolioOptimizer] Batch error:', err);\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "portfolio:cost-analyze",
      "dom": "portfolio",
      "start": 841,
      "end": 852,
      "lines": [
        "  // ── Q56: Portfolio Cost Analytics ───────────────────────────────────────\r",
        "  ipcMain.handle('portfolio:cost-analyze', async (_e, raw: unknown) => {\r",
        "    try {\r",
        "      const { positions, trades, periodDays } = raw as {\r",
        "        positions: any[]; trades: any[]; periodDays?: number;\r",
        "      };\r",
        "      const { PortfolioCostAnalytics } = await import('./engine/portfolio-cost-analytics.js');\r",
        "      const result = new PortfolioCostAnalytics().analyze(positions, trades, periodDays);\r",
        "      return { success: true, result };\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "portfolio:rar-optimize",
      "dom": "portfolio",
      "start": 855,
      "end": 867,
      "lines": [
        "  // ── Q54: RAR Optimizer ──────────────────────────────────────────────────\r",
        "  ipcMain.handle('portfolio:rar-optimize', async (_e, raw: unknown) => {\r",
        "    try {\r",
        "      const { positions, marketData, riskAppetite, constraints } = raw as {\r",
        "        positions: any[]; marketData?: any; riskAppetite?: string; constraints?: any;\r",
        "      };\r",
        "      const { RAROptimizer } = await import('./engine/rar-optimizer.js');\r",
        "      const optimizer = new RAROptimizer();\r",
        "      const result = await optimizer.optimize(positions, marketData, riskAppetite, constraints);\r",
        "      return { success: true, result };\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "portfolio:rebalance",
      "dom": "portfolio",
      "start": 908,
      "end": 921,
      "lines": [
        "  // ── Q22: Portfolio Rebalancer ────────────────────────────────────────────\r",
        "  ipcMain.handle('portfolio:rebalance', async (_e, raw: unknown) => {\r",
        "    try {\r",
        "      const { positions, targetWeights, dryRun, driftThreshold, maxTurnover } = raw as {\r",
        "        positions: any[]; targetWeights: Record<string, number>; dryRun?: boolean;\r",
        "        driftThreshold?: number; maxTurnover?: number;\r",
        "      };\r",
        "      const { getPortfolioRebalancer } = await import('./engine/portfolio-rebalancer.js');\r",
        "      const rebalancer = getPortfolioRebalancer();\r",
        "      const result = await rebalancer.rebalance(positions, targetWeights, dryRun, driftThreshold, maxTurnover);\r",
        "      return { success: true, result };\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "portfolio:rebalance-kelly",
      "dom": "portfolio",
      "start": 923,
      "end": 935,
      "lines": [
        "  ipcMain.handle('portfolio:rebalance-kelly', async (_e, raw: unknown) => {\r",
        "    try {\r",
        "      const { positions, kellyFraction, maxTurnover } = raw as {\r",
        "        positions: any[]; kellyFraction?: number; maxTurnover?: number;\r",
        "      };\r",
        "      const { getPortfolioRebalancer } = await import('./engine/portfolio-rebalancer.js');\r",
        "      const rebalancer = getPortfolioRebalancer();\r",
        "      const result = await rebalancer.kellyOptimize(positions, kellyFraction, maxTurnover);\r",
        "      return { success: true, result };\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    }
  ],
  "ws": [
    {
      "chan": "ws:connect",
      "dom": "ws",
      "start": 938,
      "end": 946,
      "lines": [
        "  // ── WebSocket Real-time Data Enhancer (JVS-58) ──────────────────────────\r",
        "  ipcMain.handle('ws:connect', async (_e, config: any) => {\r",
        "    try {\r",
        "      const result = await connectWebSocket(config);\r",
        "      return { success: result };\r",
        "    } catch (err: any) {\r",
        "      log.error('[WebSocket] Connect error:', err);\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "ws:disconnect",
      "dom": "ws",
      "start": 948,
      "end": 956,
      "lines": [
        "  ipcMain.handle('ws:disconnect', async () => {\r",
        "    try {\r",
        "      await disconnectWebSocket();\r",
        "      return { success: true };\r",
        "    } catch (err: any) {\r",
        "      log.error('[WebSocket] Disconnect error:', err);\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "ws:subscribe",
      "dom": "ws",
      "start": 958,
      "end": 966,
      "lines": [
        "  ipcMain.handle('ws:subscribe', async (_e, symbol: string) => {\r",
        "    try {\r",
        "      const result = subscribeToSymbol(symbol);\r",
        "      return { success: result };\r",
        "    } catch (err: any) {\r",
        "      log.error('[WebSocket] Subscribe error:', err);\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "ws:unsubscribe",
      "dom": "ws",
      "start": 968,
      "end": 976,
      "lines": [
        "  ipcMain.handle('ws:unsubscribe', async (_e, symbol: string) => {\r",
        "    try {\r",
        "      const result = unsubscribeFromSymbol(symbol);\r",
        "      return { success: result };\r",
        "    } catch (err: any) {\r",
        "      log.error('[WebSocket] Unsubscribe error:', err);\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "ws:subscribe-batch",
      "dom": "ws",
      "start": 978,
      "end": 986,
      "lines": [
        "  ipcMain.handle('ws:subscribe-batch', async (_e, symbols: string[]) => {\r",
        "    try {\r",
        "      const result = subscribeToSymbols(symbols);\r",
        "      return { success: true, result };\r",
        "    } catch (err: any) {\r",
        "      log.error('[WebSocket] Batch subscribe error:', err);\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "ws:unsubscribe-batch",
      "dom": "ws",
      "start": 988,
      "end": 996,
      "lines": [
        "  ipcMain.handle('ws:unsubscribe-batch', async (_e, symbols: string[]) => {\r",
        "    try {\r",
        "      const result = unsubscribeFromSymbols(symbols);\r",
        "      return { success: true, result };\r",
        "    } catch (err: any) {\r",
        "      log.error('[WebSocket] Batch unsubscribe error:', err);\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "ws:status",
      "dom": "ws",
      "start": 998,
      "end": 1006,
      "lines": [
        "  ipcMain.handle('ws:status', async () => {\r",
        "    try {\r",
        "      const status = getWebSocketStatus();\r",
        "      return { success: true, status };\r",
        "    } catch (err: any) {\r",
        "      log.error('[WebSocket] Status error:', err);\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "ws:streaming-stats",
      "dom": "ws",
      "start": 1008,
      "end": 1016,
      "lines": [
        "  ipcMain.handle('ws:streaming-stats', async () => {\r",
        "    try {\r",
        "      const stats = getStreamingStats();\r",
        "      return { success: true, stats };\r",
        "    } catch (err: any) {\r",
        "      log.error('[WebSocket] Streaming stats error:', err);\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "quote:stream-start",
      "dom": "quote",
      "start": 3157,
      "end": 3167,
      "lines": [
        "  // ── Quote Stream — Real-time Market Data (JVS-9) ─────────────────────\r",
        "  ipcMain.handle('quote:stream-start', async (_e, symbols?: string[]) => {\r",
        "    const stream = getQuoteStream();\r",
        "    if (!stream) return { success: false, error: 'QuoteStream not initialized' };\r",
        "    try {\r",
        "      if (symbols && symbols.length > 0) stream.subscribe(symbols);\r",
        "      stream.start();\r",
        "      return { success: true, status: stream.getStatus() };\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "quote:stream-stop",
      "dom": "quote",
      "start": 3169,
      "end": 3174,
      "lines": [
        "  ipcMain.handle('quote:stream-stop', async () => {\r",
        "    const stream = getQuoteStream();\r",
        "    if (!stream) return { success: false, error: 'QuoteStream not initialized' };\r",
        "    stream.stop();\r",
        "    return { success: true };\r",
        "  });\r"
      ]
    },
    {
      "chan": "quote:stream-status",
      "dom": "quote",
      "start": 3176,
      "end": 3180,
      "lines": [
        "  ipcMain.handle('quote:stream-status', async () => {\r",
        "    const stream = getQuoteStream();\r",
        "    if (!stream) return { success: false, error: 'QuoteStream not initialized' };\r",
        "    return { success: true, status: stream.getStatus() };\r",
        "  });\r"
      ]
    },
    {
      "chan": "quote:subscribe",
      "dom": "quote",
      "start": 3182,
      "end": 3187,
      "lines": [
        "  ipcMain.handle('quote:subscribe', async (_e, symbols: string[]) => {\r",
        "    const stream = getQuoteStream();\r",
        "    if (!stream) return { success: false, error: 'QuoteStream not initialized' };\r",
        "    stream.subscribe(symbols);\r",
        "    return { success: true, status: stream.getStatus() };\r",
        "  });\r"
      ]
    },
    {
      "chan": "quote:unsubscribe",
      "dom": "quote",
      "start": 3189,
      "end": 3194,
      "lines": [
        "  ipcMain.handle('quote:unsubscribe', async (_e, symbols: string[]) => {\r",
        "    const stream = getQuoteStream();\r",
        "    if (!stream) return { success: false, error: 'QuoteStream not initialized' };\r",
        "    stream.unsubscribe(symbols);\r",
        "    return { success: true, status: stream.getStatus() };\r",
        "  });\r"
      ]
    },
    {
      "chan": "push2:get-sector-heatmap",
      "dom": "push2",
      "start": 3464,
      "end": 3472,
      "lines": [
        "  // ── Push2 Proxy Service (JVS-27) ─────────────────────────────────────────\r",
        "  ipcMain.handle('push2:get-sector-heatmap', async (_e, type?: string, limit?: number) => {\r",
        "    try {\r",
        "      const proxy = getPush2Proxy();\r",
        "      const result = await proxy.getSectorHeatmap(type as any, limit);\r",
        "      return result;\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "push2:get-capital-flow-rank",
      "dom": "push2",
      "start": 3474,
      "end": 3482,
      "lines": [
        "  ipcMain.handle('push2:get-capital-flow-rank', async (_e, type?: string, limit?: number) => {\r",
        "    try {\r",
        "      const proxy = getPush2Proxy();\r",
        "      const result = await proxy.getCapitalFlowRank(type as any, limit);\r",
        "      return result;\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "push2:get-stock-quote",
      "dom": "push2",
      "start": 3484,
      "end": 3492,
      "lines": [
        "  ipcMain.handle('push2:get-stock-quote', async (_e, secid: string) => {\r",
        "    try {\r",
        "      const proxy = getPush2Proxy();\r",
        "      const result = await proxy.getStockQuote(secid);\r",
        "      return result;\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "push2:get-market-breadth",
      "dom": "push2",
      "start": 3494,
      "end": 3502,
      "lines": [
        "  ipcMain.handle('push2:get-market-breadth', async () => {\r",
        "    try {\r",
        "      const proxy = getPush2Proxy();\r",
        "      const result = await proxy.getMarketBreadth();\r",
        "      return result;\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "push2:proxy-status",
      "dom": "push2",
      "start": 3504,
      "end": 3511,
      "lines": [
        "  ipcMain.handle('push2:proxy-status', async () => {\r",
        "    try {\r",
        "      const proxy = getPush2Proxy();\r",
        "      return { success: true, status: proxy.getStatus() };\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "push2:clear-cache",
      "dom": "push2",
      "start": 3513,
      "end": 3521,
      "lines": [
        "  ipcMain.handle('push2:clear-cache', async () => {\r",
        "    try {\r",
        "      const proxy = getPush2Proxy();\r",
        "      proxy.clearCache();\r",
        "      return { success: true };\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "ws:start-stream",
      "dom": "ws",
      "start": 4035,
      "end": 4048,
      "lines": [
        "  // ── WS Data Stream (JVS-29) ────────────────────────────────────────────\r",
        "  ipcMain.handle('ws:start-stream', async (_e, config?: any) => {\r",
        "    try {\r",
        "      const stream = getWsDataStream();\r",
        "      await stream.start();\r",
        "      stream.on('tick', (tick) => {\r",
        "        if (mainWindow && !mainWindow.isDestroyed()) {\r",
        "          mainWindow.webContents.send('ws:tick', tick);\r",
        "        }\r",
        "      });\r",
        "      return { success: true };\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "ws:stop-stream",
      "dom": "ws",
      "start": 4050,
      "end": 4057,
      "lines": [
        "  ipcMain.handle('ws:stop-stream', async () => {\r",
        "    try {\r",
        "      getWsDataStream().stop();\r",
        "      return { success: true };\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "ws:subscribe",
      "dom": "ws",
      "start": 4059,
      "end": 4066,
      "lines": [
        "  ipcMain.handle('ws:subscribe', async (_e, codes: string[]) => {\r",
        "    try {\r",
        "      getWsDataStream().subscribe(codes || []);\r",
        "      return { success: true };\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "ws:unsubscribe",
      "dom": "ws",
      "start": 4068,
      "end": 4075,
      "lines": [
        "  ipcMain.handle('ws:unsubscribe', async (_e, codes: string[]) => {\r",
        "    try {\r",
        "      getWsDataStream().unsubscribe(codes || []);\r",
        "      return { success: true };\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "ws:stream-status",
      "dom": "ws",
      "start": 4077,
      "end": 4083,
      "lines": [
        "  ipcMain.handle('ws:stream-status', async () => {\r",
        "    try {\r",
        "      return { success: true, status: getWsDataStream().getStatus() };\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    }
  ],
  "backfill": [
    {
      "chan": "backfill:start",
      "dom": "backfill",
      "start": 1019,
      "end": 1027,
      "lines": [
        "  // ── Backfill Service (JVS-59) ───────────────────────────────────────────\r",
        "  ipcMain.handle('backfill:start', async (_e, config: any) => {\r",
        "    try {\r",
        "      const result = await startBackfill(config);\r",
        "      return { success: true, result };\r",
        "    } catch (err: any) {\r",
        "      log.error('[Backfill] Start error:', err);\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "backfill:stop",
      "dom": "backfill",
      "start": 1029,
      "end": 1037,
      "lines": [
        "  ipcMain.handle('backfill:stop', async () => {\r",
        "    try {\r",
        "      stopBackfill();\r",
        "      return { success: true };\r",
        "    } catch (err: any) {\r",
        "      log.error('[Backfill] Stop error:', err);\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "backfill:status",
      "dom": "backfill",
      "start": 1039,
      "end": 1047,
      "lines": [
        "  ipcMain.handle('backfill:status', async () => {\r",
        "    try {\r",
        "      const status = getBackfillStatus();\r",
        "      return { success: true, status };\r",
        "    } catch (err: any) {\r",
        "      log.error('[Backfill] Status error:', err);\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "backfill:stats",
      "dom": "backfill",
      "start": 1049,
      "end": 1057,
      "lines": [
        "  ipcMain.handle('backfill:stats', async () => {\r",
        "    try {\r",
        "      const stats = getBackfillStats();\r",
        "      return { success: true, stats };\r",
        "    } catch (err: any) {\r",
        "      log.error('[Backfill] Stats error:', err);\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "backfill:symbols",
      "dom": "backfill",
      "start": 1059,
      "end": 1067,
      "lines": [
        "  ipcMain.handle('backfill:symbols', async (_e, symbols: string[], startDate: string, endDate: string, interval?: any) => {\r",
        "    try {\r",
        "      const result = await backfillSymbols(symbols, startDate, endDate, interval);\r",
        "      return { success: true, result };\r",
        "    } catch (err: any) {\r",
        "      log.error('[Backfill] Symbols backfill error:', err);\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "backfill:incremental",
      "dom": "backfill",
      "start": 1069,
      "end": 1077,
      "lines": [
        "  ipcMain.handle('backfill:incremental', async (_e, symbol: string, startDate: string, endDate: string, existingRecords: any[]) => {\r",
        "    try {\r",
        "      const result = await incrementalBackfill(symbol, startDate, endDate, existingRecords);\r",
        "      return { success: true, result };\r",
        "    } catch (err: any) {\r",
        "      log.error('[Backfill] Incremental backfill error:', err);\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    }
  ],
  "strategy": [
    {
      "chan": "strategy:create",
      "dom": "strategy",
      "start": 1333,
      "end": 1342,
      "lines": [
        "  // ── Strategy Engine ─────────────────────────────────────────────────\r",
        "  ipcMain.handle('strategy:create', async (_e, dsl: any) => {\r",
        "    const vErr = validate(StrategyCreateSchema, { dsl });\r",
        "    if (vErr) return vErr;\r",
        "    try {\r",
        "      const id = strategyEngine?.createStrategy(dsl);\r",
        "      const strategy = strategyEngine?.getStrategy(id!);\r",
        "      if (strategy && db) db.saveStrategy(strategy);\r",
        "      return { success: true, id, strategy };\r",
        "    } catch (err: any) { return { success: false, error: err.message }; }\r",
        "  });\r"
      ]
    },
    {
      "chan": "strategy:getAll",
      "dom": "strategy",
      "start": 1344,
      "end": 1346,
      "lines": [
        "  ipcMain.handle('strategy:getAll', async () => {\r",
        "    return { success: true, strategies: strategyEngine?.getAllStrategies() || [] };\r",
        "  });\r"
      ]
    },
    {
      "chan": "strategy:get",
      "dom": "strategy",
      "start": 1348,
      "end": 1351,
      "lines": [
        "  ipcMain.handle('strategy:get', async (_e, id: string) => {\r",
        "    const strategy = strategyEngine?.getStrategy(id);\r",
        "    return { success: !!strategy, strategy };\r",
        "  });\r"
      ]
    },
    {
      "chan": "strategy:update",
      "dom": "strategy",
      "start": 1355,
      "end": 1370,
      "lines": [
        "  ipcMain.handle('strategy:update', async (_e, id: string, updates: any) => {\r",
        "    const vErr = validate(StrategyUpdateSchema, { updates });\r",
        "    if (vErr) return vErr;\r",
        "    try {\r",
        "      const strategy = strategyEngine?.getStrategy(id);\r",
        "      if (!strategy) return { success: false, error: 'Strategy not found' };\r",
        "      // Security: only allow whitelisted fields\r",
        "      const sanitized: any = {};\r",
        "      for (const key of STRATEGY_UPDATE_WHITELIST) {\r",
        "        if (key in updates) sanitized[key] = updates[key];\r",
        "      }\r",
        "      Object.assign(strategy, sanitized, { updatedAt: new Date().toISOString() });\r",
        "      if (db) db.saveStrategy(strategy);\r",
        "      return { success: true, strategy };\r",
        "    } catch (err: any) { return { success: false, error: err.message }; }\r",
        "  });\r"
      ]
    },
    {
      "chan": "strategy:delete",
      "dom": "strategy",
      "start": 1372,
      "end": 1376,
      "lines": [
        "  ipcMain.handle('strategy:delete', async (_e, id: string) => {\r",
        "    strategyEngine?.deleteStrategy(id);\r",
        "    db?.deleteStrategy(id);\r",
        "    return { success: true };\r",
        "  });\r"
      ]
    },
    {
      "chan": "strategy:backtest",
      "dom": "strategy",
      "start": 1378,
      "end": 1417,
      "lines": [
        "  ipcMain.handle('strategy:backtest', async (_e, config: any) => {\r",
        "    if (!strategyEngine || !backtestEngine) {\r",
        "      return { success: false, error: 'Engine not ready' };\r",
        "    }\r",
        "    try {\r",
        "      // Fetch K-lines\r",
        "      let klines = config.klines;\r",
        "      if (!klines || klines.length === 0) {\r",
        "        // Try cache first\r",
        "        klines = db?.getKlines(config.symbol || 'US.TQQQ', config.period || 'daily', config.count || 200);\r",
        "        if (!klines || klines.length === 0) {\r",
        "          if (opendClient?.connected) {\r",
        "            klines = await opendClient.getKlines(config.symbol || 'US.TQQQ', config.period || 'daily', config.count || 200);\r",
        "            if (klines.length > 0 && db) db.saveKlines(config.symbol || 'US.TQQQ', config.period || 'daily', klines);\r",
        "          }\r",
        "        }\r",
        "      }\r",
        "\r",
        "      if (!klines || klines.length < 50) {\r",
        "        return { success: false, error: 'K线数据不足（需要至少50根），请确认 OpenD 已连接' };\r",
        "      }\r",
        "\r",
        "      const strategyId = config.strategyId;\r",
        "      if (strategyId) {\r",
        "        const result = await strategyEngine.runBacktest(strategyId, klines);\r",
        "        if (result.success && db) {\r",
        "          db.saveBacktestResult({\r",
        "            strategyId, ...result.result,\r",
        "            initialCapital: config.initialCapital || 100000,\r",
        "          });\r",
        "        }\r",
        "        return result;\r",
        "      }\r",
        "\r",
        "      return await backtestEngine.run({ ...config, klines });\r",
        "    } catch (err: any) {\r",
        "      log.error('[IPC] Backtest error:', err.message);\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "strategy:startLive",
      "dom": "strategy",
      "start": 1419,
      "end": 1422,
      "lines": [
        "  ipcMain.handle('strategy:startLive', async (_e, strategyId: string) => {\r",
        "    strategyEngine?.startLive(strategyId);\r",
        "    return { success: true };\r",
        "  });\r"
      ]
    },
    {
      "chan": "strategy:stopLive",
      "dom": "strategy",
      "start": 1424,
      "end": 1427,
      "lines": [
        "  ipcMain.handle('strategy:stopLive', async (_e, strategyId: string) => {\r",
        "    strategyEngine?.stopLive(strategyId);\r",
        "    return { success: true };\r",
        "  });\r"
      ]
    },
    {
      "chan": "strategy:explain",
      "dom": "strategy",
      "start": 1476,
      "end": 1487,
      "lines": [
        "  // ── Strategy AI — LLM-powered (Sprint 2 P1) ─────────────────────\r",
        "  ipcMain.handle('strategy:explain', async (_e, strategy: any) => {\r",
        "    const apiKey = getDeepSeekKey_(app);\r",
        "    if (!apiKey) {\r",
        "      return { success: false, error: 'DeepSeek API key not configured. Use Settings to set your key.' };\r",
        "    }\r",
        "    const prompt = `You are a quantitative trading strategy analyst. Explain the following strategy in clear, actionable English for a retail trader.\r",
        "\r",
        "Strategy:\r",
        "- Name: ${strategy.name || 'Unnamed'}\r",
        "- Symbol: ${strategy.symbol || 'Unknown'}\r",
        "- Type: ${strategy.strategy?.type || 'Unknown'}\r",
        "- Params: ${JSON.stringify(strategy.strategy?.params || {})}}\r"
      ]
    },
    {
      "chan": "strategy:compare",
      "dom": "strategy",
      "start": 1516,
      "end": 1551,
      "lines": [
        "  ipcMain.handle('strategy:compare', async (_e, s1: any, s2: any) => {\r",
        "    const apiKey = getDeepSeekKey_(app);\r",
        "    if (!apiKey) {\r",
        "      return { success: false, error: 'DeepSeek API key not configured. Use Settings to set your key.' };\r",
        "    }\r",
        "    const fmt = (s: any) => `Name: ${s.name || '?'} | Symbol: ${s.symbol || '?'} | Type: ${s.strategy?.type || '?'} | Params: ${JSON.stringify(s.strategy?.params || {})} | SL: ${s.strategy?.stopLoss || '?'}% | TP: ${s.strategy?.takeProfit || '?'}%`;\r",
        "    const prompt = `You are a quantitative trading strategy comparison tool. Compare these two strategies objectively.\r",
        "\r",
        "Strategy A: ${fmt(s1)}\r",
        "\r",
        "Strategy B: ${fmt(s2)}\r",
        "\r",
        "Provide a structured comparison covering:\r",
        "1. Which strategy is more aggressive / conservative\r",
        "2. Which suits trending vs ranging markets\r",
        "3. Risk/reward comparison\r",
        "4. Which has better risk management (stop loss / take profit)\r",
        "5. Overall recommendation for different trader profiles\r",
        "\r",
        "Keep it under 250 words. Be objective, not promotional.`;\r",
        "\r",
        "    try {\r",
        "      const body = JSON.stringify({ model: 'deepseek-chat', messages: [{ role: 'user', content: prompt }], temperature: 0.3, max_tokens: 500 });\r",
        "      const result = await new Promise<any>((resolve, reject) => {\r",
        "        const req = require('https').request(\r",
        "          { hostname: 'api.deepseek.com', path: '/v1/chat/completions', method: 'POST', headers: { 'Content-Type': 'application/json', 'Authorization': `Bearer ${apiKey}` } },\r",
        "          (res: any) => { let data = ''; res.on('data', (c: string) => data += c); res.on('end', () => { try { resolve(JSON.parse(data)); } catch { reject(new Error('Invalid JSON')); } }); }\r",
        "        );\r",
        "        req.on('error', reject); req.write(body); req.end();\r",
        "      });\r",
        "      const content = result.choices?.[0]?.message?.content || '';\r",
        "      return { success: true, comparison: content };\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "strategy:optimize",
      "dom": "strategy",
      "start": 1554,
      "end": 1615,
      "lines": [
        "  // ── Strategy Optimizer (LLM-powered) ─────────────────────────────────\r",
        "  ipcMain.handle('strategy:optimize', async (_e, raw: unknown) => {\r",
        "    const vErr = validate(StrategyOptimizeSchema, raw);\r",
        "    if (vErr) return vErr;\r",
        "    const { strategyDSL, backtestResult } = raw as {\r",
        "      strategyDSL: { name: string; symbol?: string; type: string; params: Record<string, unknown>; stopLoss?: number; takeProfit?: number };\r",
        "      backtestResult: { totalReturn: number; sharpeRatio: number; maxDrawdown: number; winRate: number; tradeCount?: number; equityCurve?: number[] };\r",
        "    };\r",
        "    const apiKey = getDeepSeekKey_(app);\r",
        "    if (!apiKey) return { success: false, error: 'DeepSeek API key not configured. Use Settings to set your key.' };\r",
        "\r",
        "\r",
        "    const { totalReturn, sharpeRatio, maxDrawdown, winRate, tradeCount } = backtestResult;\r",
        "    const metricSummary = `Total Return: ${totalReturn}%; Sharpe: ${sharpeRatio}; Max Drawdown: ${maxDrawdown}%; Win Rate: ${winRate}%${tradeCount !== undefined ? `; Trades: ${tradeCount}` : ''}`;\r",
        "    const prompt = `You are a quantitative trading strategy optimization assistant. Based on the backtest results below, generate 3 concise parameter optimization suggestions to improve this strategy.\r",
        "\r",
        "Current Strategy:\r",
        "- Name: ${strategyDSL.name}\r",
        "- Type: ${strategyDSL.type}\r",
        "- Symbol: ${strategyDSL.symbol || 'Unknown'}\r",
        "- Current Params: ${JSON.stringify(strategyDSL.params || {})}\r",
        "- Stop Loss: ${strategyDSL.stopLoss ?? 'Not set'}%\r",
        "- Take Profit: ${strategyDSL.takeProfit ?? 'Not set'}%\r",
        "\r",
        "Backtest Results:\r",
        "${metricSummary}\r",
        "\r",
        "\r",
        "Provide exactly 3 suggestions. For each, explain:\r",
        "1. Which parameter to change and why\r",
        "2. The expected improvement\r",
        "3. A concise rationale (1 sentence)\r",
        "\r",
        "Respond ONLY with valid JSON in this exact format (no markdown, no explanation outside JSON):\r",
        "{\r",
        "  \"suggestions\": [\r",
        "    {\r",
        "      \"param\": \"param_name\",\r",
        "      \"currentValue\": \"current value or range\",\r",
        "      \"suggestedValue\": \"suggested value or range\",\r",
        "      \"reason\": \"why this improves the strategy\"\r",
        "    }\r",
        "  ]\r",
        "}`;\r",
        "\r",
        "\r",
        "    try {\r",
        "      const body = JSON.stringify({ model: 'deepseek-chat', messages: [{ role: 'user', content: prompt }], temperature: 0.4, max_tokens: 600 });\r",
        "      const result = await new Promise<any>((resolve, reject) => {\r",
        "        const req = require('https').request(\r",
        "          { hostname: 'api.deepseek.com', path: '/v1/chat/completions', method: 'POST', headers: { 'Content-Type': 'application/json', 'Authorization': `Bearer ${apiKey}` } },\r",
        "          (res: any) => { let data = ''; res.on('data', (c: string) => data += c); res.on('end', () => { try { resolve(JSON.parse(data)); } catch { reject(new Error('Invalid JSON')); } }); }\r",
        "        );\r",
        "        req.on('error', reject); req.write(body); req.end();\r",
        "      });\r",
        "      const rawContent = result.choices?.[0]?.message?.content || '';\r",
        "      let suggestions = [];\r",
        "      try { suggestions = JSON.parse(rawContent).suggestions || []; } catch { suggestions = []; }\r",
        "      return { success: true, suggestions };\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "strategy:correlation",
      "dom": "strategy",
      "start": 1618,
      "end": 1629,
      "lines": [
        "  // ── Strategy Correlation Matrix ───────────────────────────────────────\r",
        "  ipcMain.handle('strategy:correlation', async (_e, raw: unknown) => {\r",
        "    const vErr = validate(StrategyCorrelationSchema, raw);\r",
        "    if (vErr) return vErr;\r",
        "    const { strategies } = raw as {\r",
        "      strategies: { id: string; equityCurve: { time: number; value: number }[] }[];\r",
        "    };\r",
        "    if (strategies.length < 1) {\r",
        "      return { success: false, error: 'At least 1 strategy required' };\r",
        "    }\r",
        "    const result = computeCorrelationMatrix(strategies);\r",
        "    return { success: true, ...result };\r",
        "  });\r"
      ]
    },
    {
      "chan": "strategy:auto-tune",
      "dom": "strategy",
      "start": 1671,
      "end": 1686,
      "lines": [
        "  // ── Auto-Tuner ──────────────────────────────────────────────────────\r",
        "  ipcMain.handle('strategy:auto-tune', async (_e, raw: unknown) => {\r",
        "    const vErr = validate(StrategyAutoTuneSchema, raw);\r",
        "    if (vErr) return vErr;\r",
        "    const { strategyType, ranges, klines, method, populationSize, generations, iterations } = raw as {\r",
        "      strategyType: string;\r",
        "      ranges: ParamRange[];\r",
        "      klines: any[];\r",
        "      method?: 'ga' | 'bayesian' | 'both';\r",
        "      populationSize?: number;\r",
        "      generations?: number;\r",
        "      iterations?: number;\r",
        "    };\r",
        "    log.info(`[IPC] strategy:auto-tune — type=${strategyType} method=${method ?? 'both'}`);\r",
        "    const result = await autoTune(strategyType, ranges, klines, { method, populationSize, generations, iterations });\r",
        "    return { success: true, result };\r",
        "  });\r"
      ]
    },
    {
      "chan": "strategy:correlation-viz",
      "dom": "strategy",
      "start": 1819,
      "end": 1831,
      "lines": [
        "  // ── Q11: Correlation Visualizer ────────────────────────────────────\r",
        "  ipcMain.handle('strategy:correlation-viz', async (_e, raw: unknown) => {\r",
        "    try {\r",
        "      const { buildCorrelationVisualization } = require('./engine/correlation-visualizer');\r",
        "      const { inputs } = raw as { inputs: Array<{ id: string; equityCurve: Array<{ time: number; value: number }> }> };\r",
        "      if (!inputs || inputs.length < 2) {\r",
        "        return { success: false, error: 'At least 2 strategies required for correlation visualization' };\r",
        "      }\r",
        "      const result = buildCorrelationVisualization(inputs);\r",
        "      return { success: true, ...result };\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "paper:start",
      "dom": "paper",
      "start": 1834,
      "end": 1842,
      "lines": [
        "  // ── Q17: Paper Trader ─────────────────────────────────────────\r",
        "  ipcMain.handle('paper:start', async () => {\r",
        "    try {\r",
        "      const { getPaperTrader } = require('./engine/paper-trader');\r",
        "      const pt = getPaperTrader('default');\r",
        "      return { success: true, status: pt.getStatus() };\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "paper:stop",
      "dom": "paper",
      "start": 1844,
      "end": 1853,
      "lines": [
        "  ipcMain.handle('paper:stop', async () => {\r",
        "    try {\r",
        "      const { getPaperTrader } = require('./engine/paper-trader');\r",
        "      const pt = getPaperTrader('default');\r",
        "      pt.stopAll();\r",
        "      return { success: true, report: pt.getReport() };\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "paper:reset",
      "dom": "paper",
      "start": 1855,
      "end": 1863,
      "lines": [
        "  ipcMain.handle('paper:reset', async () => {\r",
        "    try {\r",
        "      const { getPaperTrader } = require('./engine/paper-trader');\r",
        "      getPaperTrader('default').reset();\r",
        "      return { success: true };\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "paper:report",
      "dom": "paper",
      "start": 1865,
      "end": 1874,
      "lines": [
        "  ipcMain.handle('paper:report', async () => {\r",
        "    try {\r",
        "      const { getPaperTrader } = require('./engine/paper-trader');\r",
        "      const pt = getPaperTrader('default');\r",
        "      const report = pt.getReport();\r",
        "      return { success: true, ...report };\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "paper:execute-signal",
      "dom": "paper",
      "start": 1876,
      "end": 1886,
      "lines": [
        "  ipcMain.handle('paper:execute-signal', async (_e, raw: unknown) => {\r",
        "    try {\r",
        "      const { getPaperTrader } = require('./engine/paper-trader');\r",
        "      const signal = raw as any;\r",
        "      const pt = getPaperTrader('default');\r",
        "      const trade = pt.executeSignal(signal, signal.name);\r",
        "      return { success: !!trade, trade };\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "paper:status",
      "dom": "paper",
      "start": 1888,
      "end": 1895,
      "lines": [
        "  ipcMain.handle('paper:status', async () => {\r",
        "    try {\r",
        "      const { getPaperTrader } = require('./engine/paper-trader');\r",
        "      return { success: true, status: getPaperTrader('default').getStatus() };\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "strategy:templates",
      "dom": "strategy",
      "start": 1914,
      "end": 1943,
      "lines": [
        "  // ── Q18: Strategy Templates ─────────────────────────────────\r",
        "  ipcMain.handle('strategy:templates', async (_e, raw: unknown) => {\r",
        "    try {\r",
        "      const { getAllTemplates, getTemplate, getTemplatesByCategory, searchTemplates, instantiateTemplate } =\r",
        "        require('./engine/strategy-templates');\r",
        "      const req = raw as { action?: string; id?: string; category?: string; query?: string; overrides?: any };\r",
        "      const action = req?.action ?? 'list';\r",
        "      if (action === 'list') return { success: true, templates: getAllTemplates() };\r",
        "      if (action === 'get') {\r",
        "        if (!req.id) return { success: false, error: 'id required' };\r",
        "        const t = getTemplate(req.id);\r",
        "        return { success: !!t, template: t };\r",
        "      }\r",
        "      if (action === 'category') {\r",
        "        if (!req.category) return { success: false, error: 'category required' };\r",
        "        return { success: true, templates: getTemplatesByCategory(req.category) };\r",
        "      }\r",
        "      if (action === 'search') {\r",
        "        if (!req.query) return { success: false, error: 'query required' };\r",
        "        return { success: true, templates: searchTemplates(req.query) };\r",
        "      }\r",
        "      if (action === 'instantiate') {\r",
        "        if (!req.id) return { success: false, error: 'id required' };\r",
        "        const { strategy, error } = instantiateTemplate(req.id, req.overrides ?? {});\r",
        "        return { success: !error, strategy, error };\r",
        "      }\r",
        "      return { success: false, error: 'Unknown action' };\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "paper:start",
      "dom": "paper",
      "start": 2165,
      "end": 2183,
      "lines": [
        "  ipcMain.handle('paper:start', async (_e, symbols?: string[]) => {\r",
        "\r",
        "    try {\r",
        "\r",
        "      const { getPaperTrader } = require('./engine/paper-trader');\r",
        "\r",
        "      const pt = getPaperTrader('default');\r",
        "\r",
        "      pt.start(symbols);\r",
        "\r",
        "      return { success: true, status: pt.getAccount() };\r",
        "\r",
        "    } catch (err: any) {\r",
        "\r",
        "      return { success: false, error: err.message };\r",
        "\r",
        "    }\r",
        "\r",
        "  });\r"
      ]
    },
    {
      "chan": "paper:stop",
      "dom": "paper",
      "start": 2187,
      "end": 2203,
      "lines": [
        "  ipcMain.handle('paper:stop', async () => {\r",
        "\r",
        "    try {\r",
        "\r",
        "      const { getPaperTrader } = require('./engine/paper-trader');\r",
        "\r",
        "      getPaperTrader('default').stop();\r",
        "\r",
        "      return { success: true };\r",
        "\r",
        "    } catch (err: any) {\r",
        "\r",
        "      return { success: false, error: err.message };\r",
        "\r",
        "    }\r",
        "\r",
        "  });\r"
      ]
    },
    {
      "chan": "paper:reset",
      "dom": "paper",
      "start": 2207,
      "end": 2223,
      "lines": [
        "  ipcMain.handle('paper:reset', async () => {\r",
        "\r",
        "    try {\r",
        "\r",
        "      const { getPaperTrader } = require('./engine/paper-trader');\r",
        "\r",
        "      getPaperTrader('default').reset();\r",
        "\r",
        "      return { success: true };\r",
        "\r",
        "    } catch (err: any) {\r",
        "\r",
        "      return { success: false, error: err.message };\r",
        "\r",
        "    }\r",
        "\r",
        "  });\r"
      ]
    },
    {
      "chan": "paper:report",
      "dom": "paper",
      "start": 2227,
      "end": 2245,
      "lines": [
        "  ipcMain.handle('paper:report', async (_e, strategyId?: string) => {\r",
        "\r",
        "    try {\r",
        "\r",
        "      const { getPaperTrader } = require('./engine/paper-trader');\r",
        "\r",
        "      const pt = getPaperTrader('default');\r",
        "\r",
        "      const report = pt.getReport(strategyId);\r",
        "\r",
        "      return { success: true, ...report };\r",
        "\r",
        "    } catch (err: any) {\r",
        "\r",
        "      return { success: false, error: err.message };\r",
        "\r",
        "    }\r",
        "\r",
        "  });\r"
      ]
    },
    {
      "chan": "paper:submit-order",
      "dom": "paper",
      "start": 2249,
      "end": 2269,
      "lines": [
        "  ipcMain.handle('paper:submit-order', async (_e, raw: unknown) => {\r",
        "\r",
        "    try {\r",
        "\r",
        "      const { getPaperTrader } = require('./engine/paper-trader');\r",
        "\r",
        "      const pt = getPaperTrader('default');\r",
        "\r",
        "      const order = raw as any;\r",
        "\r",
        "      const orderId = pt.submitOrder(order);\r",
        "\r",
        "      return { success: true, orderId };\r",
        "\r",
        "    } catch (err: any) {\r",
        "\r",
        "      return { success: false, error: err.message };\r",
        "\r",
        "    }\r",
        "\r",
        "  });\r"
      ]
    },
    {
      "chan": "strategy:multi-factor",
      "dom": "strategy",
      "start": 2417,
      "end": 2434,
      "lines": [
        "  // ── Q15: Multi-Factor Model ─────────────────────────────────────\r",
        "  ipcMain.handle('strategy:multi-factor', async (_e, raw: unknown) => {\r",
        "    try {\r",
        "      const { scoreStocks, scoreTopAStocks } = require('./engine/multi-factor');\r",
        "      const req = raw as { stocks?: Array<{ code: string; name: string }>; preset?: string; limit?: number };\r",
        "      if (req.limit != null) {\r",
        "        // Top-A mode\r",
        "        const result = await scoreTopAStocks(req.limit, req.preset as any);\r",
        "        return { success: true, ...result };\r",
        "      }\r",
        "      if (!req.stocks || req.stocks.length === 0) {\r",
        "        return { success: false, error: 'stocks array is required' };\r",
        "      }\r",
        "      const result = await scoreStocks(req as any);\r",
        "      return { success: true, ...result };\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "strategy:compare",
      "dom": "strategy",
      "start": 2466,
      "end": 2479,
      "lines": [
        "  // ── Q13: Backtest Comparator ──────────────────────────────────────\r",
        "  ipcMain.handle('strategy:compare', async (_e, raw: unknown) => {\r",
        "    try {\r",
        "      const { compareBacktests, summaryTable } = require('./engine/backtest-comparator');\r",
        "      const { results } = raw as { results: any[] };\r",
        "      if (!results || results.length === 0) {\r",
        "        return { success: false, error: 'At least one backtest result required' };\r",
        "      }\r",
        "      const comparison = compareBacktests(results);\r",
        "      const table = summaryTable(results, comparison);\r",
        "      return { success: true, comparison, table };\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "nl:parse",
      "dom": "nl",
      "start": 2482,
      "end": 2484,
      "lines": [
        "  // ── NL Parser ───────────────────────────────────────────────────────\r",
        "  ipcMain.handle('nl:parse', async (_e, text: string) => {\r",
        "    return parseNaturalLanguage(text);\r",
        "  });\r"
      ]
    },
    {
      "chan": "nl:templates",
      "dom": "nl",
      "start": 2486,
      "end": 2488,
      "lines": [
        "  ipcMain.handle('nl:templates', async () => {\r",
        "    return { success: true, templates: STRATEGY_TEMPLATES };\r",
        "  });\r"
      ]
    },
    {
      "chan": "strategy:correlation",
      "dom": "strategy",
      "start": 2784,
      "end": 2792,
      "lines": [
        "  // ── Correlation Matrix (Q2: QClaw) ──────────────────────────────────\r",
        "  ipcMain.handle('strategy:correlation', async (_e, inputs: Array<{ id: string; equityCurve: Array<{ time: number; value: number }> }>) => {\r",
        "    try {\r",
        "      const { computeCorrelationMatrix } = require('./engine/correlation-matrix');\r",
        "      const result = computeCorrelationMatrix(inputs);\r",
        "      return { success: true, ...result };\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "live:start",
      "dom": "live",
      "start": 4206,
      "end": 4217,
      "lines": [
        "  // ── Q14: Live Executor ──────────────────────────────────────────────\r",
        "  ipcMain.handle('live:start', async (_e, symbols?: string[]) => {\r",
        "    try {\r",
        "      if (!liveExecutor) {\r",
        "        return { success: false, error: 'LiveExecutor not initialized' };\r",
        "      }\r",
        "      liveExecutor.start(symbols);\r",
        "      return { success: true, status: liveExecutor.getStatus() };\r",
        "    } catch (err: any) {\r",
        "      log.error('[live:start]', err.message);\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "live:stop",
      "dom": "live",
      "start": 4219,
      "end": 4226,
      "lines": [
        "  ipcMain.handle('live:stop', async () => {\r",
        "    try {\r",
        "      liveExecutor?.stop();\r",
        "      return { success: true };\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "live:add-strategy",
      "dom": "live",
      "start": 4228,
      "end": 4238,
      "lines": [
        "  ipcMain.handle('live:add-strategy', async (_e, config: any) => {\r",
        "    try {\r",
        "      if (!liveExecutor) return { success: false, error: 'LiveExecutor not initialized' };\r",
        "      const { strategyId, symbol, signalType, price, quantity, stopLoss, takeProfit } = config;\r",
        "      if (!strategyId || !symbol) return { success: false, error: 'strategyId and symbol required' };\r",
        "      liveExecutor.addStrategy({ strategyId, symbol, signalType: signalType || 'BUY', price, quantity, stopLoss, takeProfit });\r",
        "      return { success: true };\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "live:remove-strategy",
      "dom": "live",
      "start": 4240,
      "end": 4247,
      "lines": [
        "  ipcMain.handle('live:remove-strategy', async (_e, strategyId: string) => {\r",
        "    try {\r",
        "      liveExecutor?.removeStrategy(strategyId);\r",
        "      return { success: true };\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "live:get-status",
      "dom": "live",
      "start": 4249,
      "end": 4251,
      "lines": [
        "  ipcMain.handle('live:get-status', async () => {\r",
        "    return { success: true, status: liveExecutor?.getStatus() ?? null };\r",
        "  });\r"
      ]
    },
    {
      "chan": "live:get-positions",
      "dom": "live",
      "start": 4253,
      "end": 4255,
      "lines": [
        "  ipcMain.handle('live:get-positions', async () => {\r",
        "    return { success: true, positions: liveExecutor?.getPositions() ?? [] };\r",
        "  });\r"
      ]
    },
    {
      "chan": "live:get-orders",
      "dom": "live",
      "start": 4257,
      "end": 4259,
      "lines": [
        "  ipcMain.handle('live:get-orders', async () => {\r",
        "    return { success: true, orders: liveExecutor?.getOrders() ?? [] };\r",
        "  });\r"
      ]
    }
  ],
  "backtest": [
    {
      "chan": "backtest:multiPeriod",
      "dom": "backtest",
      "start": 1431,
      "end": 1444,
      "lines": [
        "  ipcMain.handle('backtest:multiPeriod', async (_e, config: any) => {\r",
        "    const vErr = validate(BacktestMultiPeriodSchema, { config });\r",
        "    if (vErr) return vErr;\r",
        "    try {\r",
        "      const { BacktestEnhancer } = require('./engine/backtest-enhancer');\r",
        "      const enhancer = new BacktestEnhancer(backtestEngine);\r",
        "      const results = await enhancer.multiPeriodBacktest(\r",
        "        config.klines, config.strategyConfig, config.periods\r",
        "      );\r",
        "      return { success: true, results };\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "backtest:paramSweep",
      "dom": "backtest",
      "start": 1446,
      "end": 1459,
      "lines": [
        "  ipcMain.handle('backtest:paramSweep', async (_e, config: any) => {\r",
        "    const vErr = validate(BacktestParamSweepSchema, { config });\r",
        "    if (vErr) return vErr;\r",
        "    try {\r",
        "      const { BacktestEnhancer } = require('./engine/backtest-enhancer');\r",
        "      const enhancer = new BacktestEnhancer(backtestEngine);\r",
        "      const results = await enhancer.parameterSweep(\r",
        "        config.klines, config.baseConfig, config.paramRanges, config.maxCombinations || 100\r",
        "      );\r",
        "      return { success: true, results };\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "backtest:riskMetrics",
      "dom": "backtest",
      "start": 1464,
      "end": 1473,
      "lines": [
        "  ipcMain.handle('backtest:riskMetrics', async (_e, equityCurve: number[], riskFreeRate?: number) => {\r",
        "    try {\r",
        "      const { BacktestEnhancer } = require('./engine/backtest-enhancer');\r",
        "      const enhancer = new BacktestEnhancer(backtestEngine);\r",
        "      const metrics = enhancer.computeDeepRiskMetrics(equityCurve, riskFreeRate || 0.03);\r",
        "      return { success: true, metrics };\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "backtest:stability",
      "dom": "backtest",
      "start": 1707,
      "end": 1725,
      "lines": [
        "  // ── Q64: Backtest Stability Checker ──────────────────────────────────\r",
        "  ipcMain.handle('backtest:stability', async (_e, raw: unknown) => {\r",
        "    try {\r",
        "      const { isReturns, oosReturns, paramGridResults, walkForwardResults, isPeriodDays, oosPeriodDays, tradingDays } = raw as {\r",
        "        isReturns: number[];\r",
        "        oosReturns: number[];\r",
        "        paramGridResults?: any[];\r",
        "        walkForwardResults?: any[];\r",
        "        isPeriodDays?: number;\r",
        "        oosPeriodDays?: number;\r",
        "        tradingDays?: number;\r",
        "      };\r",
        "      const { BacktestStabilityChecker } = await import('./engine/backtest-stability.js');\r",
        "      const checker = new BacktestStabilityChecker();\r",
        "      const result = checker.analyzeStability({ isReturns, oosReturns, paramGridResults, walkForwardResults, isPeriodDays, oosPeriodDays, tradingDays });\r",
        "      return { success: true, ...result };\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "backtest:walk-forward",
      "dom": "backtest",
      "start": 4137,
      "end": 4152,
      "lines": [
        "  // ── Walk-Forward Analysis (Sprint 2 — JVS) ───────────────────────────\r",
        "  ipcMain.handle('backtest:walk-forward', async (_e, config: any) => {\r",
        "    const vErr = validate(BacktestWalkForwardSchema, { config });\r",
        "    if (vErr) return vErr;\r",
        "    try {\r",
        "      const wfa = new WalkForwardEngine();\r",
        "      const klines = config.klines || [];\r",
        "      if (klines.length < 100) {\r",
        "        return { success: false, error: 'K线数据不足 (需至少100根)' };\r",
        "      }\r",
        "      const report = await wfa.run(config, klines);\r",
        "      return { success: true, report };\r",
        "    } catch (err: any) {\r",
        "      log.error('[WFA] Failed:', err.message);\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "backtest:param-scan",
      "dom": "backtest",
      "start": 4155,
      "end": 4170,
      "lines": [
        "  // ── Parameter Scanner (Sprint 2 — JVS) ───────────────────────────────\r",
        "  ipcMain.handle('backtest:param-scan', async (_e, config: any) => {\r",
        "    const vErr = validate(BacktestParamScanSchema, { config });\r",
        "    if (vErr) return vErr;\r",
        "    try {\r",
        "      const scanner = new ParameterScanner();\r",
        "      const klines = config.klines || [];\r",
        "      if (klines.length < 50) {\r",
        "        return { success: false, error: 'K线数据不足 (需至少50根)' };\r",
        "      }\r",
        "      const report = await scanner.run({ ...config, klines });\r",
        "      return { success: true, report };\r",
        "    } catch (err: any) {\r",
        "      log.error('[ParamScan] Failed:', err.message);\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "backtest:multi-timeframe",
      "dom": "backtest",
      "start": 4173,
      "end": 4203,
      "lines": [
        "  // ── Multi-timeframe comparison (Sprint 2 — JVS) ──────────────────────\r",
        "  ipcMain.handle('backtest:multi-timeframe', async (_e, config: any) => {\r",
        "    const vErr = validate(BacktestMultiTimeframeSchema, { config });\r",
        "    if (vErr) return vErr;\r",
        "    try {\r",
        "      const engine = new BacktestEngine();\r",
        "      const timeframes = config.timeframes || ['1m', '5m', '15m', '1h', 'daily'];\r",
        "      const results: Record<string, any> = {};\r",
        "\r",
        "      for (const tf of timeframes) {\r",
        "        const klines = config.klinesByTimeframe?.[tf] || [];\r",
        "        if (klines.length < 50) {\r",
        "          results[tf] = { success: false, error: 'K线不足' };\r",
        "          continue;\r",
        "        }\r",
        "        const btResult = await engine.run({\r",
        "          symbol: config.symbol,\r",
        "          initialCapital: config.initialCapital || 100000,\r",
        "          commission: config.commission || 0.001,\r",
        "          slippage: config.slippage || 0.0005,\r",
        "          strategy: config.strategy,\r",
        "          klines,\r",
        "        });\r",
        "        results[tf] = btResult;\r",
        "      }\r",
        "\r",
        "      return { success: true, results, timeframes };\r",
        "    } catch (err: any) {\r",
        "      log.error('[MultiTF] Failed:', err.message);\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    }
  ],
  "system": [
    {
      "chan": "system:opend-health",
      "dom": "system",
      "start": 1898,
      "end": 1911,
      "lines": [
        "  // ── Q19: OpenD Health Check ─────────────────────────────────\r",
        "  ipcMain.handle('system:opend-health', async (_e, raw: unknown) => {\r",
        "    try {\r",
        "      const { runOpenDHealthCheck, pingOpenD } = require('./engine/opend-health-check');\r",
        "      const req = raw as { action?: string; host?: string; port?: number };\r",
        "      if (req?.action === 'ping') {\r",
        "        const result = await pingOpenD(req.host ?? '127.0.0.1', req.port ?? 11111);\r",
        "        return { success: true, ...result };\r",
        "      }\r",
        "      const result = await runOpenDHealthCheck(req as any);\r",
        "      return { success: true, ...result };\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    }
  ],
  "snapshot": [
    {
      "chan": "snapshot:capture",
      "dom": "snapshot",
      "start": 1946,
      "end": 1953,
      "lines": [
        "  // ── JVS-39: Data Snapshot Service ──────────────────────────────────────\r",
        "  ipcMain.handle('snapshot:capture', async (_e, type: string, category: string, data: any, metadata?: any) => {\r",
        "    try {\r",
        "      const snapshot = await captureSnapshot(type, category, data, metadata);\r",
        "      return { success: true, snapshot };\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "snapshot:query",
      "dom": "snapshot",
      "start": 1955,
      "end": 1962,
      "lines": [
        "  ipcMain.handle('snapshot:query', async (_e, query: any) => {\r",
        "    try {\r",
        "      const snapshots = await querySnapshots(query);\r",
        "      return { success: true, snapshots };\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "snapshot:get",
      "dom": "snapshot",
      "start": 1964,
      "end": 1971,
      "lines": [
        "  ipcMain.handle('snapshot:get', async (_e, id: string) => {\r",
        "    try {\r",
        "      const snapshot = await getSnapshot(id);\r",
        "      return { success: true, snapshot };\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "snapshot:compare",
      "dom": "snapshot",
      "start": 1973,
      "end": 1980,
      "lines": [
        "  ipcMain.handle('snapshot:compare', async (_e, id1: string, id2: string) => {\r",
        "    try {\r",
        "      const comparison = await compareSnapshots(id1, id2);\r",
        "      return { success: true, comparison };\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "snapshot:timeline",
      "dom": "snapshot",
      "start": 1982,
      "end": 1989,
      "lines": [
        "  ipcMain.handle('snapshot:timeline', async (_e, category: string, limit?: number) => {\r",
        "    try {\r",
        "      const timeline = await getSnapshotTimeline(category, limit);\r",
        "      return { success: true, timeline };\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "snapshot:latest",
      "dom": "snapshot",
      "start": 1991,
      "end": 1998,
      "lines": [
        "  ipcMain.handle('snapshot:latest', async (_e, category: string) => {\r",
        "    try {\r",
        "      const snapshot = await getLatestSnapshot(category);\r",
        "      return { success: true, snapshot };\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "snapshot:cleanup",
      "dom": "snapshot",
      "start": 2000,
      "end": 2007,
      "lines": [
        "  ipcMain.handle('snapshot:cleanup', async (_e, daysOld?: number) => {\r",
        "    try {\r",
        "      const deleted = await cleanupOldSnapshots(daysOld);\r",
        "      return { success: true, deleted };\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "snapshot:export",
      "dom": "snapshot",
      "start": 2009,
      "end": 2016,
      "lines": [
        "  ipcMain.handle('snapshot:export', async (_e, query?: any) => {\r",
        "    try {\r",
        "      const json = await exportSnapshots(query);\r",
        "      return { success: true, json };\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "snapshot:import",
      "dom": "snapshot",
      "start": 2018,
      "end": 2025,
      "lines": [
        "  ipcMain.handle('snapshot:import', async (_e, jsonString: string) => {\r",
        "    try {\r",
        "      const imported = await importSnapshots(jsonString);\r",
        "      return { success: true, imported };\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "snapshot:stats",
      "dom": "snapshot",
      "start": 2027,
      "end": 2034,
      "lines": [
        "  ipcMain.handle('snapshot:stats', async () => {\r",
        "    try {\r",
        "      const stats = getSnapshotStats();\r",
        "      return { success: true, stats };\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "snapshot:delete",
      "dom": "snapshot",
      "start": 2036,
      "end": 2043,
      "lines": [
        "  ipcMain.handle('snapshot:delete', async (_e, id: string) => {\r",
        "    try {\r",
        "      const deleted = await deleteSnapshot(id);\r",
        "      return { success: true, deleted };\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "snapshot:clear",
      "dom": "snapshot",
      "start": 2045,
      "end": 2052,
      "lines": [
        "  ipcMain.handle('snapshot:clear', async () => {\r",
        "    try {\r",
        "      await clearAllSnapshots();\r",
        "      return { success: true };\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    }
  ],
  "version": [
    {
      "chan": "version:track",
      "dom": "version",
      "start": 2055,
      "end": 2062,
      "lines": [
        "  // ── JVS-40: Version Control Service ──────────────────────────────────────\r",
        "  ipcMain.handle('version:track', async (_e, entityId: string, entityType: string, data: any, changeType?: string, changeSummary?: string, userId?: string, tags?: string[]) => {\r",
        "    try {\r",
        "      const version = await trackVersion(entityId, entityType, data, changeType as any, changeSummary, userId, tags);\r",
        "      return { success: true, version };\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "version:get-entity-versions",
      "dom": "version",
      "start": 2064,
      "end": 2071,
      "lines": [
        "  ipcMain.handle('version:get-entity-versions', async (_e, entityId: string, limit?: number) => {\r",
        "    try {\r",
        "      const versions = await getEntityVersions(entityId, limit);\r",
        "      return { success: true, versions };\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "version:get",
      "dom": "version",
      "start": 2073,
      "end": 2080,
      "lines": [
        "  ipcMain.handle('version:get', async (_e, versionId: string) => {\r",
        "    try {\r",
        "      const version = await getVersion(versionId);\r",
        "      return { success: true, version };\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "version:get-latest",
      "dom": "version",
      "start": 2082,
      "end": 2089,
      "lines": [
        "  ipcMain.handle('version:get-latest', async (_e, entityId: string) => {\r",
        "    try {\r",
        "      const version = await getLatestVersion(entityId);\r",
        "      return { success: true, version };\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "version:diff",
      "dom": "version",
      "start": 2091,
      "end": 2098,
      "lines": [
        "  ipcMain.handle('version:diff', async (_e, versionId1: string, versionId2: string) => {\r",
        "    try {\r",
        "      const diff = await diffVersions(versionId1, versionId2);\r",
        "      return { success: true, diff };\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "version:rollback",
      "dom": "version",
      "start": 2100,
      "end": 2107,
      "lines": [
        "  ipcMain.handle('version:rollback', async (_e, entityId: string, targetVersion: number) => {\r",
        "    try {\r",
        "      const result = await rollback(entityId, targetVersion);\r",
        "      return { success: true, result };\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "version:query",
      "dom": "version",
      "start": 2109,
      "end": 2116,
      "lines": [
        "  ipcMain.handle('version:query', async (_e, query: any) => {\r",
        "    try {\r",
        "      const versions = await queryVersions(query);\r",
        "      return { success: true, versions };\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "version:stats",
      "dom": "version",
      "start": 2118,
      "end": 2125,
      "lines": [
        "  ipcMain.handle('version:stats', async () => {\r",
        "    try {\r",
        "      const stats = getVersionStats();\r",
        "      return { success: true, stats };\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "version:delete",
      "dom": "version",
      "start": 2127,
      "end": 2134,
      "lines": [
        "  ipcMain.handle('version:delete', async (_e, versionId: string) => {\r",
        "    try {\r",
        "      const deleted = await deleteVersion(versionId);\r",
        "      return { success: true, deleted };\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "version:clear",
      "dom": "version",
      "start": 2136,
      "end": 2143,
      "lines": [
        "  ipcMain.handle('version:clear', async () => {\r",
        "    try {\r",
        "      await clearAllVersions();\r",
        "      return { success: true };\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "version:export",
      "dom": "version",
      "start": 2145,
      "end": 2152,
      "lines": [
        "  ipcMain.handle('version:export', async (_e, query?: any) => {\r",
        "    try {\r",
        "      const json = await exportVersions(query);\r",
        "      return { success: true, json };\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "version:import",
      "dom": "version",
      "start": 2154,
      "end": 2161,
      "lines": [
        "  ipcMain.handle('version:import', async (_e, jsonString: string) => {\r",
        "    try {\r",
        "      const imported = await importVersions(jsonString);\r",
        "      return { success: true, imported };\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    }
  ],
  "db": [
    {
      "chan": "db:getStrategies",
      "dom": "db",
      "start": 2527,
      "end": 2529,
      "lines": [
        "  // ── Database ────────────────────────────────────────────────────────\r",
        "  ipcMain.handle('db:getStrategies', async () => {\r",
        "    return db?.getStrategies() || [];\r",
        "  });\r"
      ]
    },
    {
      "chan": "db:saveStrategy",
      "dom": "db",
      "start": 2531,
      "end": 2534,
      "lines": [
        "  ipcMain.handle('db:saveStrategy', async (_e, strategy: any) => {\r",
        "    db?.saveStrategy(strategy);\r",
        "    return { success: true };\r",
        "  });\r"
      ]
    },
    {
      "chan": "db:getSettings",
      "dom": "db",
      "start": 2536,
      "end": 2538,
      "lines": [
        "  ipcMain.handle('db:getSettings', async () => {\r",
        "    return db?.getSettings() || {};\r",
        "  });\r"
      ]
    },
    {
      "chan": "db:saveSettings",
      "dom": "db",
      "start": 2540,
      "end": 2543,
      "lines": [
        "  ipcMain.handle('db:saveSettings', async (_e, settings: any) => {\r",
        "    db?.saveSettings(settings);\r",
        "    return { success: true };\r",
        "  });\r"
      ]
    },
    {
      "chan": "db:getTrades",
      "dom": "db",
      "start": 2545,
      "end": 2547,
      "lines": [
        "  ipcMain.handle('db:getTrades', async (_e, strategyId?: string) => {\r",
        "    return db?.getTrades(strategyId) || [];\r",
        "  });\r"
      ]
    },
    {
      "chan": "db:getBacktestResults",
      "dom": "db",
      "start": 2549,
      "end": 2551,
      "lines": [
        "  ipcMain.handle('db:getBacktestResults', async (_e, strategyId: string) => {\r",
        "    return db?.getBacktestResults(strategyId) || [];\r",
        "  });\r"
      ]
    },
    {
      "chan": "db:getWatchlist",
      "dom": "db",
      "start": 2553,
      "end": 2555,
      "lines": [
        "  ipcMain.handle('db:getWatchlist', async () => {\r",
        "    return db?.getWatchlist() || [];\r",
        "  });\r"
      ]
    },
    {
      "chan": "db:saveWatchlist",
      "dom": "db",
      "start": 2557,
      "end": 2560,
      "lines": [
        "  ipcMain.handle('db:saveWatchlist', async (_e, codes: string[]) => {\r",
        "    db?.saveWatchlist(codes);\r",
        "    return { success: true };\r",
        "  });\r"
      ]
    },
    {
      "chan": "db:getSignals",
      "dom": "db",
      "start": 2562,
      "end": 2564,
      "lines": [
        "  ipcMain.handle('db:getSignals', async (_e, strategyId?: string) => {\r",
        "    return db?.getSignals(strategyId) || [];\r",
        "  });\r"
      ]
    }
  ],
  "app": [
    {
      "chan": "app:getInfo",
      "dom": "app",
      "start": 2567,
      "end": 2575,
      "lines": [
        "  // ── App ─────────────────────────────────────────────────────────────\r",
        "  ipcMain.handle('app:getInfo', () => ({\r",
        "    version: app.getVersion(),\r",
        "    name: 'DAWN WHALES',\r",
        "    platform: process.platform,\r",
        "    arch: process.arch,\r",
        "    electronVersion: process.versions.electron,\r",
        "    nodeVersion: process.versions.node,\r",
        "    chromeVersion: process.versions.chrome,\r",
        "  }));\r"
      ]
    },
    {
      "chan": "app:getMemoryUsage",
      "dom": "app",
      "start": 2577,
      "end": 2580,
      "lines": [
        "  ipcMain.handle('app:getMemoryUsage', () => ({\r",
        "    mainProcess: Math.round(process.memoryUsage().heapUsed / 1024 / 1024),\r",
        "    total: Math.round(process.memoryUsage().rss / 1024 / 1024),\r",
        "  }));\r"
      ]
    },
    {
      "chan": "app:exportPdf",
      "dom": "app",
      "start": 2582,
      "end": 2610,
      "lines": [
        "  ipcMain.handle('app:exportPdf', async (_e, filename: string) => {\r",
        "    try {\r",
        "      if (!mainWindow || mainWindow.isDestroyed()) {\r",
        "        return { success: false, error: 'Window not available' };\r",
        "      }\r",
        "      const { dialog } = require('electron');\r",
        "      const fs = require('fs');\r",
        "      const path = require('path');\r",
        "\r",
        "      const defaultPath = path.join(require('os').homedir(), 'Downloads', filename);\r",
        "      const { filePath, canceled } = await dialog.showSaveDialog(mainWindow, {\r",
        "        defaultPath,\r",
        "        filters: [{ name: 'PDF', extensions: ['pdf'] }],\r",
        "      });\r",
        "      if (canceled || !filePath) return { success: false, error: 'User cancelled' };\r",
        "\r",
        "      const data = await mainWindow.webContents.printToPDF({\r",
        "        marginsType: 1,\r",
        "        pageSize: 'A4',\r",
        "        printBackground: true,\r",
        "        preferCSSPageSize: true,\r",
        "      });\r",
        "      fs.writeFileSync(filePath, data);\r",
        "      return { success: true, path: filePath };\r",
        "    } catch (err: any) {\r",
        "      log.error('[App] PDF export failed:', err.message);\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "app:emergencyStop",
      "dom": "app",
      "start": 2612,
      "end": 2635,
      "lines": [
        "  ipcMain.handle('app:emergencyStop', async () => {\r",
        "    try {\r",
        "      log.warn('[App] Emergency stop triggered');\r",
        "      // Stop all live strategies\r",
        "      const strategies = strategyEngine?.getAllStrategies() || [];\r",
        "      for (const s of strategies) {\r",
        "        if (s.liveRunning) {\r",
        "          strategyEngine?.stopLive(s.id);\r",
        "        }\r",
        "      }\r",
        "      // Notify renderer\r",
        "      if (mainWindow && !mainWindow.isDestroyed()) {\r",
        "        mainWindow.webContents.send('notification', {\r",
        "          type: 'error',\r",
        "          title: '紧急停止',\r",
        "          message: '所有策略已停止',\r",
        "        });\r",
        "      }\r",
        "      return { success: true };\r",
        "    } catch (err: any) {\r",
        "      log.error('[App] Emergency stop failed:', err.message);\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "app:openExternal",
      "dom": "app",
      "start": 2639,
      "end": 2653,
      "lines": [
        "  ipcMain.handle('app:openExternal', async (_e, rawUrl: string) => {\r",
        "    const vErr = validate(Z.object({ rawUrl: z.string().url() }), { rawUrl });\r",
        "    if (vErr) return vErr;\r",
        "    try {\r",
        "      const url = new URL(rawUrl);\r",
        "      if (!ALLOWED_PROTOCOLS.includes(url.protocol)) {\r",
        "        log.warn('[Security] Blocked openExternal:', rawUrl);\r",
        "        return { success: false, error: 'Protocol not allowed' };\r",
        "      }\r",
        "      await shell.openExternal(rawUrl);\r",
        "      return { success: true };\r",
        "    } catch {\r",
        "      return { success: false, error: 'Invalid URL' };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "app:getVersion",
      "dom": "app",
      "start": 2655,
      "end": 2656,
      "lines": [
        "  ipcMain.handle('app:getVersion', () => app.getVersion());\r",
        "  ipcMain.handle('app:getPlatform', () => process.platform);\r"
      ]
    },
    {
      "chan": "app:getPlatform",
      "dom": "app",
      "start": 2656,
      "end": 2657,
      "lines": [
        "  ipcMain.handle('app:getPlatform', () => process.platform);\r",
        "\r"
      ]
    },
    {
      "chan": "app:checkUpdate",
      "dom": "app",
      "start": 2659,
      "end": 2666,
      "lines": [
        "  // ── Auto-updater ──────────────────────────────────────────────────\r",
        "  ipcMain.handle('app:checkUpdate', async () => {\r",
        "    try {\r",
        "      const result = await autoUpdater.checkForUpdates();\r",
        "      return { success: true, version: result?.updateInfo?.version || null };\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "app:downloadUpdate",
      "dom": "app",
      "start": 2668,
      "end": 2675,
      "lines": [
        "  ipcMain.handle('app:downloadUpdate', async () => {\r",
        "    try {\r",
        "      await autoUpdater.downloadUpdate();\r",
        "      return { success: true };\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "app:installUpdate",
      "dom": "app",
      "start": 2677,
      "end": 2679,
      "lines": [
        "  ipcMain.handle('app:installUpdate', () => {\r",
        "    autoUpdater.quitAndInstall();\r",
        "  });\r"
      ]
    }
  ],
  "marketplace": [
    {
      "chan": "marketplace:rate",
      "dom": "marketplace",
      "start": 2728,
      "end": 2738,
      "lines": [
        "  ipcMain.handle('marketplace:rate', async (_e, strategyId: string, rating: number) => {\r",
        "    const vErr = validate(MarketplaceRateSchema, { strategyId });\r",
        "    if (vErr) return vErr;\r",
        "    try {\r",
        "      db?.rateStrategy(strategyId, rating);\r",
        "      const stats = db?.getStrategyRating(strategyId);\r",
        "      return { success: true, ...stats };\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "marketplace:getRating",
      "dom": "marketplace",
      "start": 2740,
      "end": 2744,
      "lines": [
        "  ipcMain.handle('marketplace:getRating', async (_e, strategyId: string) => {\r",
        "    const rating = db?.getStrategyRating(strategyId);\r",
        "    const myRating = db?.getMyRating(strategyId);\r",
        "    return { success: true, ...rating, myRating };\r",
        "  });\r"
      ]
    },
    {
      "chan": "marketplace:comment",
      "dom": "marketplace",
      "start": 2746,
      "end": 2755,
      "lines": [
        "  ipcMain.handle('marketplace:comment', async (_e, strategyId: string, content: string, parentId?: number) => {\r",
        "    const vErr = validate(MarketplaceCommentSchema, { strategyId });\r",
        "    if (vErr) return vErr;\r",
        "    try {\r",
        "      db?.addComment(strategyId, content, parentId);\r",
        "      return { success: true };\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "marketplace:getComments",
      "dom": "marketplace",
      "start": 2757,
      "end": 2760,
      "lines": [
        "  ipcMain.handle('marketplace:getComments', async (_e, strategyId: string) => {\r",
        "    const comments = db?.getComments(strategyId) || [];\r",
        "    return { success: true, comments };\r",
        "  });\r"
      ]
    },
    {
      "chan": "marketplace:savePerformance",
      "dom": "marketplace",
      "start": 2762,
      "end": 2771,
      "lines": [
        "  ipcMain.handle('marketplace:savePerformance', async (_e, data: any) => {\r",
        "    const vErr = validate(MarketplaceSavePerformanceSchema, { data });\r",
        "    if (vErr) return vErr;\r",
        "    try {\r",
        "      db?.saveStrategyPerformance(data);\r",
        "      return { success: true };\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "marketplace:getPerformance",
      "dom": "marketplace",
      "start": 2773,
      "end": 2776,
      "lines": [
        "  ipcMain.handle('marketplace:getPerformance', async (_e, strategyId: string) => {\r",
        "    const perf = db?.getStrategyPerformance(strategyId) || [];\r",
        "    return { success: true, performance: perf };\r",
        "  });\r"
      ]
    },
    {
      "chan": "marketplace:list",
      "dom": "marketplace",
      "start": 2778,
      "end": 2781,
      "lines": [
        "  ipcMain.handle('marketplace:list', async (_e, sortBy?: string, limit?: number) => {\r",
        "    const strategies = db?.getMarketplaceStrategies(sortBy || 'rating', limit || 50) || [];\r",
        "    return { success: true, strategies };\r",
        "  });\r"
      ]
    },
    {
      "chan": "marketplace:score",
      "dom": "marketplace",
      "start": 2795,
      "end": 2804,
      "lines": [
        "  // ── Marketplace: Score & Verify (JVS) ─────────────────────────────────\r",
        "  ipcMain.handle('marketplace:score', async (_e, strategyId: string) => {\r",
        "    if (!marketplaceService) return { success: false, error: 'MarketplaceService not initialized' };\r",
        "    try {\r",
        "      const score = marketplaceService.calculateStrategyScore(strategyId);\r",
        "      return { success: true, score };\r",
        "    } catch (err: any) {\r",
        "      log.error('[Marketplace] Score calculation failed:', err.message);\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "marketplace:verify",
      "dom": "marketplace",
      "start": 2806,
      "end": 2815,
      "lines": [
        "  ipcMain.handle('marketplace:verify', async (_e, strategyId: string) => {\r",
        "    if (!marketplaceService) return { success: false, error: 'MarketplaceService not initialized' };\r",
        "    try {\r",
        "      const verification = marketplaceService.verifyPerformance(strategyId);\r",
        "      return { success: true, verification };\r",
        "    } catch (err: any) {\r",
        "      log.error('[Marketplace] Verification failed:', err.message);\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "marketplace:updateAllScores",
      "dom": "marketplace",
      "start": 2817,
      "end": 2826,
      "lines": [
        "  ipcMain.handle('marketplace:updateAllScores', async () => {\r",
        "    if (!marketplaceService) return { success: false, error: 'MarketplaceService not initialized' };\r",
        "    try {\r",
        "      const result = marketplaceService.updateAllScores();\r",
        "      return { success: true, ...result };\r",
        "    } catch (err: any) {\r",
        "      log.error('[Marketplace] Batch update failed:', err.message);\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    }
  ],
  "cache": [
    {
      "chan": "cache:explore",
      "dom": "cache",
      "start": 3739,
      "end": 3746,
      "lines": [
        "  // ── Cache Explorer API (JVS-35) ───────────────────────────────────────\r",
        "  ipcMain.handle('cache:explore', async () => {\r",
        "    try {\r",
        "      const result = exploreCache();\r",
        "      return { success: true, result };\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "cache:entry-detail",
      "dom": "cache",
      "start": 3748,
      "end": 3755,
      "lines": [
        "  ipcMain.handle('cache:entry-detail', async (_e, namespace: string, key: string) => {\r",
        "    try {\r",
        "      const detail = getCacheEntryDetail(namespace, key);\r",
        "      return { success: true, detail };\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "cache:keys-paginated",
      "dom": "cache",
      "start": 3757,
      "end": 3764,
      "lines": [
        "  ipcMain.handle('cache:keys-paginated', async (_e, namespace: string, limit?: number, offset?: number) => {\r",
        "    try {\r",
        "      const result = getCacheKeys(namespace, limit || 100, offset || 0);\r",
        "      return { success: true, result };\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "cache:get",
      "dom": "cache",
      "start": 3846,
      "end": 3855,
      "lines": [
        "  // ── Smart Cache Manager (JVS-32) ──────────────────────────────────────\r",
        "  ipcMain.handle('cache:get', async (_e, namespace: string, key: string) => {\r",
        "    try {\r",
        "      const manager = getSmartCacheManager();\r",
        "      const cache = manager.getCache(namespace);\r",
        "      const value = cache.get(key);\r",
        "      return { success: true, value, hit: value !== undefined };\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "cache:set",
      "dom": "cache",
      "start": 3857,
      "end": 3866,
      "lines": [
        "  ipcMain.handle('cache:set', async (_e, namespace: string, key: string, value: any, ttl?: number) => {\r",
        "    try {\r",
        "      const manager = getSmartCacheManager();\r",
        "      const cache = manager.getCache(namespace);\r",
        "      cache.set(key, value, ttl);\r",
        "      return { success: true };\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "cache:has",
      "dom": "cache",
      "start": 3868,
      "end": 3876,
      "lines": [
        "  ipcMain.handle('cache:has', async (_e, namespace: string, key: string) => {\r",
        "    try {\r",
        "      const manager = getSmartCacheManager();\r",
        "      const cache = manager.getCache(namespace);\r",
        "      return { success: true, exists: cache.has(key) };\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "cache:delete",
      "dom": "cache",
      "start": 3878,
      "end": 3886,
      "lines": [
        "  ipcMain.handle('cache:delete', async (_e, namespace: string, key: string) => {\r",
        "    try {\r",
        "      const manager = getSmartCacheManager();\r",
        "      const cache = manager.getCache(namespace);\r",
        "      return { success: true, deleted: cache.delete(key) };\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "cache:clear",
      "dom": "cache",
      "start": 3888,
      "end": 3900,
      "lines": [
        "  ipcMain.handle('cache:clear', async (_e, namespace?: string) => {\r",
        "    try {\r",
        "      const manager = getSmartCacheManager();\r",
        "      if (namespace) {\r",
        "        manager.clearNamespace(namespace);\r",
        "      } else {\r",
        "        manager.clearAll();\r",
        "      }\r",
        "      return { success: true };\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "cache:stats",
      "dom": "cache",
      "start": 3902,
      "end": 3914,
      "lines": [
        "  ipcMain.handle('cache:stats', async (_e, namespace?: string) => {\r",
        "    try {\r",
        "      const manager = getSmartCacheManager();\r",
        "      if (namespace) {\r",
        "        const cache = manager.getCache(namespace);\r",
        "        return { success: true, stats: cache.getStats() };\r",
        "      } else {\r",
        "        return { success: true, stats: manager.getAllStats() };\r",
        "      }\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "cache:reset-stats",
      "dom": "cache",
      "start": 3916,
      "end": 3929,
      "lines": [
        "  ipcMain.handle('cache:reset-stats', async (_e, namespace?: string) => {\r",
        "    try {\r",
        "      const manager = getSmartCacheManager();\r",
        "      if (namespace) {\r",
        "        const cache = manager.getCache(namespace);\r",
        "        cache.resetStats();\r",
        "      } else {\r",
        "        manager.resetAllStats();\r",
        "      }\r",
        "      return { success: true };\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    },
    {
      "chan": "cache:keys",
      "dom": "cache",
      "start": 3931,
      "end": 3939,
      "lines": [
        "  ipcMain.handle('cache:keys', async (_e, namespace: string) => {\r",
        "    try {\r",
        "      const manager = getSmartCacheManager();\r",
        "      const cache = manager.getCache(namespace);\r",
        "      return { success: true, keys: cache.keys() };\r",
        "    } catch (err: any) {\r",
        "      return { success: false, error: err.message };\r",
        "    }\r",
        "  });\r"
      ]
    }
  ]
}