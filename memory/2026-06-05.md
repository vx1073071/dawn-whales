# Daily Memory — 2026-06-05

## 22:13 — Round 16 完成

### P0-4: RiskEngine 单元测试 (Q95) ✅
- 15/15 tests PASS ✅
- 测试文件：`tests/q95-risk-engine.test.ts`
- 覆盖：checkOrder / calculatePositionSize / getDrawdownState / getVolatilityFactor / getKellyStats / getConfig / getAlerts / getStatusSnapshot
- **重要教训**：必须先读源码确认真实签名，再写测试！假设签名导致多次失败
  - `calculatePositionSize(price, atr?, stopPrice?)` 不是 `(config object)`
  - `getKellyStats()` 无参数，返回 `{ winRate, avgWin, avgLoss, profitFactor, kellyFraction, sampleSize }`
  - `getDrawdownState()` 返回 `{ peakEquity, currentDrawdownPct, maxDrawdownPct, isReduced, reductionFactor }`

### P0-5: QTest 文档化 ✅
- 创建 `docs/QTEST.md` (7275 bytes)
- 覆盖：API参考、matcher列表、架构说明、运行方式、CLI选项、报告格式、迁移指南
- **关键实现细节**（.not 双重否定修复）：`.not` getter 创建新 Expect 对象而非 toggle flag

### QTest Framework Bug Fix ✅
- `.not` 双重否定修复：`get not()` 改为 `createExpect(received, { not: true })`（硬编码 true），避免 `!config.not` toggle 导致双重否定

### JVS-83 Data Aggregator ✅
- 17/17 PASS
- 修复：`fetchSources` → `fetchFromSources`（匹配测试 spy）
- 修复：test config `timeout` → `timeoutMs`

### Bridge API Integration Tests (P0-2/3) ✅
- `tests/jvs-bridge-api.test.ts`：21/21 PASS
- 修复：mock 泄漏、klines timestamp 字段、calculateGreeks mock

### 当前测试状态
- 全量：404 pass / 29 fail（9 suites）
- 所有失败均为历史遗留：EventEmitter extends、missing imports 等
- 与本次 Round 16 改动无关

### 今日 commits
- `135129ef` — fix(Q17): EventEmitter composition pattern
- `7577cd8c` — feat(qtest): .not fix + risk-engine unit tests + bridge-api integration tests + P0-4/5 done
