# quant-moo v0.12.0 Release Notes

**Release Date**: 2026-06-07
**Codename**: Phase 6.3 — Marketplace + 性能 + 技术债务清理
**Build**: 0 errors / 0 warnings

## 🎉 Phase 6.3 启动

v0.12.0 标志 **Phase 6.3 完善**。从 v0.11.0 (Phase 6.2 启动) 升级。

## 📊 测试指标

- **3054 passed / 0 failed / 9 skipped (173 files)**
- **11.7× growth** from v0.7.0 baseline
- **5 轮稳定性** 验证

## 🦐 5 虾完成情况

| 虾 | 任务 | 状态 |
|------|------|:--:|
| **ML** | Marketplace 前端 + PWA 优化 + 移动端手势 | ✅ |
| **JVS** | StrategyMarketplaceSearch + 数据管道健康 | ✅ |
| **QClaw** | 5 轮回归 + Lighthouse + E2E | ✅ |
| **PM** | v0.12.0 发布 + 守护 | ✅ |
| **dao** | Code Review + 4 文档 | ✅ |

## 🔧 PM 守护修复 (4 大类 R46)

### 1. graph-neural-network 补全
- `getConfig()` / `getMetrics()` / `getNode()` / `reset()` / `analyzeRisk()` / `detectAnomalies()`
- `getMetrics` 加 `avgDegree` + `density` + `volatilityRisk` 字段
- 修复 `}` 早闭合 + 重复 `return [...rebalanceHistory]` 语法错误
- addNode 智能 derive features (从 returns 派生)

### 2. nlp-sentiment-engine 补全
- `getConfig()` / `getMetrics()` / `analyzeSentiment()` / `aggregateSentiment()` / `reset()`
- 修复 analyze 接受 NewsArticle 对象 (`text.match is not a function`)
- 修复 negation 用字边界 (排除 "未来" 中的 "未")
- scoreToLabel 改 `positive/negative/neutral` (适配测试)
- 词典补 "超出" "超出预期"

### 3. reinforcement-learning-agent 新建 (212L)
- 完整 Q-Learning + epsilon-greedy 实现
- `getConfig()` / `getMetrics()` / `setEpsilon()` / `discretizeState()` / `train()` / `reset()`
- 支持 terminal state (done=true)
- MarketState 接口扩展 (returns/rsi/macd/smaCross)

### 4. package.json 升级
- `0.11.0 → 0.12.0` (R45 漏改, R46 必修)

## 📈 9 轮增长轨迹 (R38-R46)

```
R38: 1593 | R39: 1775 | R40: 1955+v0.8.0 | R41: 2076+v0.8.1-alpha
R42: 2238+v0.9.0 | R43: 2400+v0.9.1-alpha | R44: 2596+v0.10.0
R45: 2797+v0.11.0 | R46: 3054+v0.12.0
```

涨幅 92% (R38→R46) | 9 轮 9 个发布版本

## 🚀 安装 / 升级

### 新用户
- 下载 `quant-moo-0.12.0-windows-x64.exe` (Windows)
- macOS / Linux: 源代码构建

### 升级用户
- 应用内自动更新 (Phase 6.0 已实装)
- 数据迁移自动完成

## 🐛 已知问题

无 — 5 轮 0 fail 验证

## 🙏 致谢

5 虾协作主副双岗 (ML/JVS/QClaw/PM/dao) — 9 轮稳定推进。

— PM (WorkBuddy)
